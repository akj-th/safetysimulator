# AI 안전 진단 시스템 — 프로젝트 안내

> 이 파일은 Claude Code가 작업을 시작할 때 **자동으로 먼저 읽는** 프로젝트 안내문입니다.
> 프로젝트가 바뀌면 이 파일도 계속 업데이트하세요.

## 1. 이 프로젝트는 무엇인가

- **발주/주관**: 건축공간연구원(AURI) — "7대 사회재난 다중취약 공간 진단 및 AI 맞춤형 안전 솔루션 개발 지원 연구" (국가 위탁 연구).
- **최종 목표**: 공무원이 사용하는 앱. 도시 공간의 안전 취약성을 진단하고, 물리적 개선안을 제안·시각화한다.
- **우리 회사(ATELIER-KJ)의 담당 모듈**:
  거리 이미지 입력 → AI가 취약 요소 진단 → 규칙 기반으로 개선 시설물 처방 → 개선 전/후 이미지 생성.
- **담당자**: 태환 (건축 전공, 개발 비전공).

## 2. 핵심 파이프라인 (4단계)

1. **입력** — 스트리트뷰 이미지 또는 현장에서 찍은 사진 한 장.
2. **진단** — 정해진 체크리스트(7대 사회재난 분야)로 "무엇이 없고 무엇이 위험한지" 판독. (AI 모델, 추후 연결)
3. **처방** — 진단 결과를 규칙표에 대입해 알맞은 시설물을 선정. (결정론적 규칙 엔진)
4. **시각화** — 확정된 시설물만 원본 이미지 위에 렌더링해 개선 후 모습을 보여줌.

## 3. 반드시 지킬 설계 원칙

- **처방은 규칙 엔진이 정한다.** 어떤 시설물을 넣을지를 이미지 생성 모델이 결정하게 하지 말 것.
- **감사 가능성**: 최종 산출물은 예산 산출의 근거가 되는 행정 문서다. 모든 처방은 "왜 이 시설물인가"를 진단 근거로 역추적할 수 있어야 한다.
- **규칙 기반 처방과 생성형 렌더링을 분리해서 유지**한다. (둘을 섞지 말 것)
- **AI는 새로 학습시키지 않는다.** 이미 있는 모델을 우리 규칙·기준으로 "설정해서" 쓴다. 이 프로젝트의 핵심 가치는 AI 성능이 아니라 "문제 → 시설물" 규칙표(건축·도시설계 전문성)에 있다.

## 4. 제약·유의사항

- **폐쇄망 vs 온라인 API**: 사업 시행계획서는 폐쇄망을 전제하는데, 지도·스트리트뷰 API는 온라인이다. (이은석 박사 확인 중) 데모 단계는 온라인으로 진행하되, 최종 납품은 사전 캐싱/오프라인 방식을 고려한다.
- **스트리트뷰 = 진단의 입력 이미지 그 자체.** 단순 배경이 아니다.
- **라이선스**: 스트리트뷰 이미지를 변형한 결과물의 2차 저작물 문제에 유의.

## 5. 현재 상태 (코드)

별도의 "진단 시뮬레이터" 데모 파일은 없다 — 아래 3개 파일이 곧 그 데모이며, 클로드코드로 이어서 만드는 중이다.

- **index.html** (위치 선택): 네이버 지도(NCP Web Dynamic Map)로 주소 검색·클릭해 취약 지점을 고르고, 그 지점의 스트리트뷰를 보여준다. 8개 분야 119 응급신고 이력 참고 레이어(더미 데이터, 분야별 토글)도 지도 위에 표시. "이 위치로 진단 시작" → diagnosis.html로 이동.
- **diagnosis.html** (진단 결과): 좌표 기반 시드로 만든 7대 재난 유형별 더미 점수를 카드로 보여줌. 스트리트뷰 화면 캡처 또는 직접 촬영 사진 업로드로 "진단 입력 이미지"를 고정할 수 있음(캡처는 브라우저 보안 정책상 실패할 수 있어 업로드가 폴백). "종합 리포트 보기" → report.html로 이동.
- **report.html** (종합 리포트): 항목별 더미 점수를 평균 내 종합 위험도 요약 + 진단 입력 이미지 + 항목별 리스트를 보여줌. "개선 제안" 영역은 다음 단계에서 채울 자리.
- **다음 작업(예정)**: 8개 분야 119 SHP 데이터 수신 후 더미 레이어를 실제 데이터로 교체, AI 진단 로직 연결, 규칙 기반 처방 엔진 연결.
- GitHub Pages로 배포 중. 저장소: `akj-th/safetysimulator`.

## 6. 기술 스택

- 순수 HTML / CSS / JavaScript (프레임워크 없음 — 비전공자가 유지보수하기 쉽도록 단순하게 유지).
- 네이버 지도 API (네이버 클라우드 플랫폼). 로드 방식:
  `oapi.map.naver.com/openapi/v3/maps.js?ncpKeyId=<Client ID>&submodules=geocoder,panorama`
  (지도=Web Dynamic Map, 주소검색=geocoder, 스트리트뷰=panorama)
- 배포: GitHub Pages. 저장소: `akj-th/safetysimulator`.
- 지도가 뜨려면 네이버 콘솔의 "Web 서비스 URL"에 배포 주소(github.io)가 등록돼 있어야 한다.

## 7. 작업 방식 (중요)

- **설명은 쉬운 우리말로.** 담당자는 개발 비전공이므로, 전문용어는 풀어서 쓰고 필요하면 비유로 설명한다.
- **코드를 바꾸기 전에** 무엇을, 왜 바꾸는지 한두 줄로 먼저 설명한다.
- **과하게 복잡하게 만들지 않는다.** 지금은 핵심 흐름을 먼저 검증하는 단계다.
- **가장 위험한 가정을 먼저 검증**하고(예: 렌더링 품질), 단계적으로 확장한다.
- 큰 방향(전체 구조·설계) 판단은 담당자와 함께 정한다. 임의로 아키텍처를 크게 바꾸지 않는다.
