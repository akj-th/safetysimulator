# AI 안전 진단 시스템 — 프로젝트 안내

> 이 파일은 Claude Code가 작업을 시작할 때 **자동으로 먼저 읽는** 프로젝트 안내문입니다.
> 프로젝트가 바뀌면 이 파일도 계속 업데이트하세요.
> (근거 원문: AURI 위탁용역 시행계획서)

---

## 1. 한눈에 보기

- **우리(ATELIER-KJ)가 만드는 것**: 거리 이미지로 도시 공간의 안전 취약성을 진단하고, 규칙 기반으로 개선 시설물을 처방한 뒤, 개선 전/후 이미지와 행정 문서를 만들어주는 프로토타입 앱.
- **왜**: 공무원이 취약 공간을 빠르게 진단하고, 안전사업지구 지정을 위한 국비 신청·예산 배분을 지원하기 위해.
- **담당자**: 태환 (건축 전공, 개발 비전공 — 설명은 쉬운 우리말로).

---

## 2. 상위 과제 개요 (발주 배경)

- **상위 과제명**: 「안전사업지구 사업모델 개발 연구용역」
- **주관/발주**: 건축공간연구원(AURI) / 주무부처 행정안전부 예방안전제도과
- **근거**: 재난안전기본법 제66조의13(안전사업지구의 지정 및 지원), 국정과제 72·73
- **과제기간**: 2026.05.04 ~ 2027.01.29 (최종 사업평가 2026.12 예정)
- **과제예산**: 2억 6,700만원 (₩267,000,000) — ※ 우리 위탁용역 예산은 별도(아래 3번)
- **정책 배경**: 지역안전지수 고도화. '26년부터 안전진단 대상을 전국 41개 취약 시·군·구로 확대. 기존 '단일 취약점 관리'에서 → 7대 사회재난을 종합 모니터링하는 패러다임으로 전환.

---

## 3. 우리 과업(위탁용역) 개요

- **과업명**: 7대 사회재난 다중취약 공간 진단 및 AI 맞춤형 안전솔루션 개발지원 연구
- **기간**: 계약일로부터 6개월
- **예산**: 5천만원 (₩50,000,000)
- **시행자**: Atelier KJ (연구책임자 김경재 소장, 연구원 2인, 연구보조원 1인)
- **목적**: 7대 사회재난이 다중 중첩되는 공간을 미시적으로 진단 → 41개 대상지의 물리적 예방 인프라 표준 가이드 수립 → 실무자의 신속한 국비 신청·예산 배분을 지원하는 LLM 기반 통합 AI 솔루션 프로토타입 앱 개발.

### 7대 사회재난
자살 · 교통사고 · 화재 · 범죄 · 생활안전 · 산업재해 · 감염병

### 공간 단위·대상지
- 진단 단위: **100 × 100m 격자** (거시적 시·군·구 통계가 아니라 미시적 공간)
- 대상: 2026년 지역안전진단 컨설팅 대상 **41개 기초지자체** (서울 관악/금천, 인천 옹진/**미추홀구**, 경기 부천/포천, 부산 동구/사상/영도 등 — 전체 목록은 시행계획서 참조)
- **현재 데모 대상: 인천 미추홀구** (여러 재난 지표가 함께 높은 다중취약 구간)

---

## 4. 공식 4대 과업 (시행계획서 기준)

> 아래가 계약상 우리가 납품해야 하는 범위입니다.
> 지금 만드는 **거리뷰 기반 앱은 이 중 과업 2·3·4를 잇는 실행 수단**입니다.

**과업 1 — 7대 재난 통합 데이터 모델 + 백엔드 매칭 알고리즘**
- 분절된 7대 분야 원시데이터를 100×100m 격자 단위로 연계·매핑하는 표준 데이터 구조. (외부 유출 불가한 지자체 내부 보안 환경 전제 → 로컬 데이터 관리 체계)
- 위치별 7대 분야 위해 수준·행정 노력을 정량 연산해 발주팀의 **HEA 4대 진단 유형(Type A·B·C·D)** 으로 자동 분류·매칭.

**과업 2 — 안전사업 인프라 배분 시뮬레이터 (UI/UX)**
- 지자체당 안전사업지구 예산 한도(**개소당 약 10억 원 내외**) 기준, 실무자가 재난 분야별 예산 투자 비중을 직관적으로 조정하는 시뮬레이터.
- 조정 시 격자 취약도와 연계된 표준 시설물의 소요 수량·배치 가능 공간을 실시간 연산해 **지도(오프라인 구동 고려) 상에 시각화**.
- **독립 구동형 프로토타입**: 외부 네트워크 없이 내부망에 단독 설치·구동되는 반응형 웹/모바일 Mock-up + 결과 리포트 출력.

**과업 3 — 폐쇄망 대응 AI 진단 문서 생성 모듈**
- 상용 오픈 API를 쓸 수 없는 **폐쇄망 서버 전제**. 오프라인 독자 구동 경량 LLM(sLLM) + 로컬 추론 엔진.
- 진단 결과 + 시설물 투입 정보 → 안전사업지구 지정 서식(**지정 신청 사유서, 예산 산출 근거, 기대효과**)에 부합하는 표준 텍스트 자동 생성.
- **할루시네이션 방지** + 법정 재난안전 용어 정확 반영 위해 비공개 행정 지침서·단가 표준을 참조하는 데이터 연계(RAG 등). 최종 소스코드 패키징 제출.

**과업 4 — 재난 예방 인프라 표준 설계 가이드라인**
- 7대 재난별 생활권 하드웨어 인프라 설계 지침:
  - 자살·범죄: 옥상 자동개폐장치, 안전 펜스, CPTED 시설 등
  - 교통·생활안전: 바닥형 보행신호등, 핸드레일 등
  - 화재·산재·감염병: 스마트 소화전, 안전 사이니지, 스마트 클린 쉘터 등
- 다중 위험 중첩 공간을 유형화한 **복합 매뉴얼 + 표준 단가 매트릭스** (단일 재난 대책의 매몰 비용 방지).
- 시뮬레이터 결과와 매칭해 즉시 인용 가능한 **시설물별 표준 도면 시안 + 공간 배치 예시도**.

---

## 5. 우리가 만드는 핵심 파이프라인 (4단계)

1. **입력** — 스트리트뷰 또는 현장 사진 한 장 (지도에서 위치 선택).
2. **진단** — 정해진 체크리스트(7대 분야)로 취약 요소 판독 → HEA Type A~D 매칭. (AI, 추후 연결)
3. **처방** — 진단 결과를 규칙표(과업 4의 시설물·표준단가)에 대입해 시설물 선정. (결정론적 규칙 엔진)
4. **시각화·문서** — 확정된 시설물만 원본 이미지에 렌더링 + 사유서·예산 근거 등 행정 문서(과업 3) 출력.

---

## 6. 반드시 지킬 설계 원칙

- **처방은 규칙 엔진이 정한다.** 어떤 시설물을 넣을지를 이미지 생성 모델이 결정하게 하지 말 것.
- **감사 가능성**: 최종 산출물은 예산 산출의 근거가 되는 행정 문서다. 모든 처방은 "왜 이 시설물인가"를 진단 근거로 역추적할 수 있어야 한다. (시행계획서의 할루시네이션 방지 요건과 직결)
- **규칙 기반 처방과 생성형 렌더링을 분리해서 유지**한다.
- **AI는 새로 학습시키지 않는다.** 기존 모델을 우리 규칙·기준으로 "설정해서" 쓴다. 핵심 가치는 AI 성능이 아니라 "문제 → 시설물" 규칙표(건축·도시설계 전문성)에 있다.

---

## 7. 제약·유의사항

- **폐쇄망 vs 온라인 API (최대 쟁점)**: 시행계획서는 "외부 네트워크 연결 없이 내부망 단독 구동"과 "상용 오픈 API 사용 불가"를 명시. 반면 네이버 지도·스트리트뷰는 온라인 호출. → **데모는 온라인으로 진행하되, 최종 납품은 사전 캐싱/오프라인 지도 방식으로 전환 전제.** (이은석 박사 확인 중)
- **스트리트뷰 = 진단의 입력 이미지 그 자체.** 단순 배경이 아니다.
- **지도 시각화도 "오프라인 구동 환경 고려"** 가 시행계획서에 명시됨.
- **소스코드 패키징 제출**이 요건 → 특정 클라우드 전용 방식에 의존하지 말 것.
- **라이선스**: 스트리트뷰 이미지를 변형한 결과물의 2차 저작물 문제에 유의. 캡처해서 AI에 넣고 변형본을 행정 문서에 첨부하는 구조이므로, 지도 사업자 약관 확인 필요. 최종본은 **현장 촬영 사진**으로 가야 할 수도 있음.

### 지도 API 전환 검토 결과 (2026-08, 결론: 네이버 유지)

카카오맵 앱에 캡처 기능이 있어 전환을 검토했으나, **API로는 불가능**한 것을 확인했습니다. 같은 고민을 반복하지 않도록 기록해 둡니다.

| | 로드뷰 렌더링 | 직접 캡처 |
|---|---|---|
| 네이버 | canvas 사용 | ❌ 다른 서버 이미지라 canvas 오염 → 저장 차단 |
| 카카오 | **img 8장을 3D로 조립** | ❌ **canvas 자체가 없음** |

- 카카오맵 **앱**의 캡처는 앱 기능이며 **JavaScript SDK와 무관**합니다.
- 검증 도구: `tools/kakao-capture-test.html` (카카오 JS 키 필요). 정책이 바뀌면 이걸로 재확인.
- 따라서 **화면공유 방식 캡처를 유지**합니다. 어차피 폐쇄망 최종본에서는 온라인 지도를 못 쓰므로, 이 부분은 데모 편의 문제입니다.

---

## 8. 현재 상태 (코드)

5단계가 한 줄로 이어져 끝까지 돕니다. 페이지 사이의 데이터 전달은 `sessionStorage`를 씁니다.

| 단계 | 파일 | 상태 |
|---|---|---|
| 1. 입력 | `index.html` | **실제 작동** — 네이버 지도(NCP)로 지점 선택, **실제 119 출동 이력 레이어**, 좌표→읍면동 조회 |
| 2. 진단 | `diagnosis.html` + `server/` | **실제 작동** — 캡처·업로드한 사진을 AI가 체크리스트로 판독. 서버가 꺼져 있으면 더미 점수로 자동 대체 |
| 3. 리포트 | `report.html` | **실제 작동** — **행안부 요구 6단 구성** + 읍면동 종합 분석 + 결정론적 규칙 엔진 |
| 4. 시각화 | `visualize.html` | **실제 작동** — 개선 후 이미지를 Gemini/GPT로 생성. 품질 검증 중 |
| 5. 문서 | `document.html` | **문서 2종** — **현장용 리포트(먼저) → 사전진단서**(AURI 양식 5장). 탭으로 나뉘며 각각 A4 인쇄. 빈칸은 편집칸 + AI 초안 |
| 로그인 | `login.html` · `admin.html` + `server/auth.js` | **계정 로그인 + 관리자 승인** (2026-09-14). 로그인 전에는 서버가 화면을 내주지 않음 |
| 프로젝트 | `projects.html` + `assets/projects.js` + `server/projects.js` | **진단을 계정별로 저장·불러오기** (2026-09-15). 관리자는 전체 열람 |

### 리포트 6단 구성 (2026-08-25 행안부 협의 · 주무관 보완사항 검토)

행안부 지적: *"100m 격자로 종속변수·설명변수 상관성을 검증하는 연구보고서 형태는
실무자에게 아무 도움이 안 된다. 인적 특성 + 물적·공간환경 + 사고원인/유형을
구체적 통계치와 함께 직관적으로 제시하라."*

전체 흐름은 **데이터 결과 → 위험요인 해석 → 현장확인 사항 → 맞춤형 사업**입니다.

| 단 | 내용 | 어디서 나오는가 |
|---|---|---|
| ① 인적 특성 | 연령·성별, 조사지 내 vs 지자체 전체 배수 | `data/stats/` (119 출동자료) |
| ② 사고유형 | 발생 장소·유형·시간대 집중 | `data/stats/` |
| ③ 물적·공간환경 | 그 자리에 무엇이 있는가 | 사진 AI 판독 (기존) |
| ④ 대응역량 | 소방·경찰·AED·CCTV | **자료 미수신 — "확인 필요" 표기** |
| ⑤ 현장확인 사항 | 현장에서 볼 것 | 사진 판독 + 출동자료 |
| ⑥ 개선방향 | 맞춤형 사업 | 규칙 엔진 (`rules.js`) |

- 리포트 **최상단은 읍면동 종합 분석**입니다. 주무관이 담당 동 단위로 사업을 관리하기 때문입니다(9/1 회의). 동 이름은 1단계에서 좌표를 행정구역으로 조회해 넘겨받습니다(`picked.dong`).
- 서술 대상은 **강원대가 확정한 중점 3분야**뿐입니다. 7개 분야를 균등하게 다루지 않습니다.
- **조사지 밖 지점**은 "조사지 내 vs 전체" 비교가 성립하지 않으므로 지자체 전체 기준으로 서술합니다.

### 진단 → 처방 연결 (항목별 판독) ★ 이 앱의 핵심

**시설물을 고르는 것은 분야 점수가 아니라 "사진에서 문제로 확인된 체크리스트 항목"입니다.**

```
사진 → 항목마다 예/아니오/확인불가 → 문제 항목이 시설물을 부름 → 처방
        (CRM-1 가로등 있는가 = 아니오)   (LED 보안등 · CPTED 조명)
```

- 같은 점수라도 **사진이 다르면 처방이 다릅니다.** 범죄 80점이어도 "가로등 없음"과 "CCTV 없음"은 전혀 다른 시설물이 나옵니다.
- 여러 항목이 같은 시설물을 불러도 **한 번만** 들어갑니다.

**개수는 판정이 정합니다** (`RX_MAX_BY_LEVEL`)

| 판정 | 처방 개수 |
|---|---|
| 위험 (67~100) | **2종** |
| 주의 (34~66) | **1종** |
| 안전 (0~33) | 0종 |

예) 7개 분야가 위험 2 · 주의 4 · 안전 1 → `2×2 + 4×1` = **8종**. 사진에서 문제가 아무리 많이 잡혀도 개수는 이 규칙을 넘지 않습니다.

**어느 것을 뽑을지는 적합도(0~100)가 정합니다** (`auriFitScore`)

"이 사진에 이 시설물이 얼마나 들어맞는가"를 AI의 항목별 판독에서 **규칙으로 계산**합니다. AI에게 시설물 점수를 직접 묻지 않는 것은, 처방 근거가 되짚어지지 않으면 감사 추적이 깨지기 때문입니다.

1. **분야 점수가 높을수록** — 문제가 심한 곳일수록 잘 들어맞음
2. **그 문제의 1순위 대책일수록** — `byItem` 배열의 앞자리. 뒷자리는 같은 문제를 푸는 **대안**이므로 적합도가 절반으로 떨어집니다 (`점수 ÷ (순위+1)`)
3. **여러 항목에서 함께 확인될수록** — 근거가 겹치면 항목당 +8점 (최대 +15)

분야 안에서 이 점수로 줄을 세워 **상위 N개**만 뽑습니다. 점수가 같으면 규칙표에 적힌 순서를 따릅니다.

**필수/권장도 적합도로 갈립니다** — `RX_MUST_FIT`(67) 이상이면 필수. 위험/주의를 가르는 경계와 같은 값이라, 보통 위험 분야의 1순위는 필수, 그 분야의 2순위(대안)나 주의 분야는 권장이 됩니다.

근거 줄에 적합도가 함께 찍혀서 **왜 이것이 뽑혔고 왜 필수인지**를 되짚을 수 있습니다.
- 처방 근거 줄에 **어느 항목이 이 시설물을 불렀는지**가 찍힙니다:
  `근거: 사진 판독 CRM-1 — 가로등 미설치 → 규칙 R-CRM-01 · 범죄 위험 판정 · 표준단가 95만원/개소`

**세 파일이 번호로 맞물립니다. 하나만 고치면 조용히 끊깁니다.**

| 파일 | 무엇을 정하는가 |
|---|---|
| `server/checklist.js` | 항목 번호(`CRM-1`) · 질문 · **어느 답이 문제인가**(`risk: 'yes'\|'no'`) |
| `server/server.js` | AI가 항목마다 답하도록 강제, 답에 "문제 있음" 표시를 붙여 전달 |
| `assets/rules.js` | `byItem` — 항목 번호 → 시설물 |

- 체크리스트에 항목을 추가하면 `rules.js`의 `byItem`에도 그 번호를 넣어야 처방에 반영됩니다. 안 넣으면 **점수에만** 반영됩니다.
- `risk` 폴라리티 때문에 **질문은 반드시 예/아니오로 답할 수 있어야** 합니다. ("관리 상태가 어떠한가" 같은 문장 금지)
- 점검: `node tools/verify-rules.mjs` (또는 `npm run verify-rules --prefix tools`) — 세 파일의 번호·이름·단가·개입 유형이 어긋나지 않는지 확인합니다 (항목 양방향 일치, 62종 전부 단가·유형 보유). checklist.js나 rules.js를 고칠 때마다 돌려 보세요.

**도구 명령 정리** (`tools/` 폴더에서)
| 명령 | 하는 일 | 언제 |
|---|---|---|
| `npm run build` | 119 CSV → `data/incidents/` + `data/stats/` | 새 119 자료를 받으면 |
| `npm run palette` | 사업 팔레트 엑셀 → `assets/palette.json` | **새 팔레트를 받으면** |
| `npm run physical` | 물적환경 회귀분석 → `assets/physical.json` | 새 회귀분석 결과를 받으면 |
| `npm run compare-kangwon` | 강원대 연령·성별 ↔ 우리 계산값 대조 | 강원대 자료가 갱신되면 |
| `npm run compare-kangwon-type` | **A_type 검증** — 우리 통계의 강원대 분류 ↔ 강원대 0904 사고유형 표 (41곳 × 7분야) + 시트 정렬 점검 | build 뒤 |
| `npm run dong-hea` | **동별 HEA 취약도** → `data/dong/<지역>.json` (H·E·A·종합 + 근거 + 동 경계선) | build 뒤 · 경계·TIF·회귀분석이 바뀌면 |
| `npm run heatmap` | 위험도 GeoTIFF → `data/heatmap/` PNG 287장 | 새 밀도분석 결과를 받으면 |
| `npm run verify` | 만든 통계가 AURI 값과 맞는지 295건 대조 | build 뒤 |
| `npm run verify-rules` | 체크리스트 ↔ 연결표 ↔ 팔레트 정합성 | **palette 뒤 · 연결표를 고친 뒤** |

### 위험도 히트맵 (`data/heatmap/`) — ⏸ 보류 (2026-09-07)

> **AURI 화상회의에서 보류로 정해졌습니다.** 면으로 덮으면 어디가 문제인지 짚기 어렵고,
> 격자 점이 낫다는 의견입니다. **코드와 PNG는 지웠습니다가 아니라 그대로 둡니다.**
>
> | | |
> |---|---|
> | 지도 화면 | `index.html` 을 커밋 `ea424ab` 로 되돌려 히트맵 UI 를 뺐습니다 |
> | 남겨 둔 것 | `tools/build-heatmap.mjs` · `tools/lib/geotiff.mjs` · `tools/lib/png.mjs` · 서버 화이트리스트 한 줄 |
> | **지운 것** | `data/heatmap/` PNG 287장 + index.json (**19MB**) — 저장소 용량·배포 속도 때문 |
> | 되살리려면 | `npm run heatmap --prefix tools` 로 PNG 를 다시 만들고, `index.html` 의 히트맵 부분을 되돌리기 전 것으로 넣으면 됩니다 (둘 다 git 이력에 있음) |

⚠️ 원본 TIF(`data/raw/gis_0901/density/`, 6.6GB)가 있어야 다시 만들 수 있습니다. 저장소에는 없습니다.

아래는 만들 때의 기록입니다.

강원대가 QGIS로 만든 커널밀도 분석 결과를 지도에 얹습니다.
**밀도값은 우리가 다시 계산하지 않습니다** — 재계산하면 사전진단서에 실린 히트맵과 다른 그림이 나옵니다. 축소·색칠·좌표 계산만 합니다.

| | |
|---|---|
| 원본 | `data/raw/gis_0901/density/` — **6.6GB**, 🔴 커밋 금지 |
| 결과 | `data/heatmap/*.png` 287장 — 커밋 대상 |
| 해상도 | 원본 10m → 4배 축소 **40m** (`SHRINK`) |
| 색 기준 | 그 지자체·그 분야 안의 **99분위**를 만점 (`TOP_QUANTILE`) |
| 색 방식 | **흰색 → 분야 색** 그래디언트 + 투명도 (`COLOR_GAMMA` · `ALPHA_FULL_AT`) |
| 용량 | PNG 287장 **18.2MB** |

**⚠️ 원본이 두 형식으로 섞여 있습니다.** 헤더를 직접 확인한 값입니다.

| 폴더 | 자료형 | 압축 | NoData |
|---|---|---|---|
| `교통사고_최종` | float64 | 없음 | **−3.4e38 (음수)** |
| 나머지 6개 분야 | float32 | **LZW** | +3.4e38 (양수) |

NoData 부호가 반대라 한쪽 기준만 쓰면 다른 쪽이 새까맣게 나옵니다. **절댓값으로 판정**합니다. LZW는 TIFF 방식(MSB 우선·코드폭 조기 증가)으로 직접 구현했고, 안동 교통사고_구급의 99분위 103.22·최대 304.32가 AURI 확인값과 일치해 검증했습니다.

**⚠️ 교통사고는 `교통사고_최종`(TrafficRisk)을 씁니다.** `density/설명.txt`가 "구급 + 다발지역을 합친 최종"이라고 명시하기 때문입니다. 다만 이 파일은 **0~1로 정규화된 위험지수**라 다른 분야(원시 밀도값)와 성격이 다릅니다. 분야별로 따로 정규화하므로 화면에서는 문제가 없지만, 값을 직접 비교하면 안 됩니다. 바꾸려면 `build-heatmap.mjs`의 `FOLDER.traffic` 한 줄입니다.

**화면**: 레이어 목록 맨 위가 **종합 위험도 · 위험도 히트맵** 두 줄입니다.

히트맵은 아래 7개 분야의 **표시 방식을 바꾸는 스위치**입니다.

| 히트맵 | 7개 분야 | 종합 위험도 |
|---|---|---|
| 꺼짐 | 격자 **점** | 점 |
| 켜짐 | 밀도 **면** (점은 감춤) | 점 (그대로) |

점과 면을 함께 켜면 서로를 가려 아무것도 읽을 수 없어 **둘 중 하나만** 올립니다. 종합 위험도는 히트맵이 없는(우리가 계산한) 레이어라 언제나 점입니다.

켠 분야의 **PNG 한 장만** 그때 내려받습니다(287장을 미리 받지 않습니다). 투명도 슬라이더 기본 60%.

⚠️ 히트맵이 없는 지자체로 옮기면 스위치가 자동으로 꺼집니다 — 켜진 채로 두면 점도 면도 없어 지도가 텅 빕니다.

**색은 상대값입니다.** 그 지자체·그 분야 안에서의 순위이므로 지역 간·분야 간 색 비교는 할 수 없습니다.

**`fallback` — 항목 판독이 없을 때만** 쓰는 예비 목록입니다. 서버가 꺼져 더미 점수로 돌 때, 예전 세션 데이터를 열었을 때 예전 방식(분야 단위)으로 처방합니다. 지우지 마세요.

⚠️ **연구원이 판독 근거 글(`observed`)을 고쳐도 처방은 바뀌지 않습니다.** 처방을 바꾸려면 **재판독(`/api/revise`)** 을 쓰세요 — 그쪽은 항목 답까지 다시 받습니다.

⚠️ AI가 분야당 요약 하나가 아니라 **항목 43개에 각각 답하므로** 출력 토큰과 호출 시간이 늘어납니다.

### AI 진단 구조 (2단계)

```
브라우저(diagnosis.html)  →  중계 서버(server/)  →  AI
                                ↑ API 키는 여기에만 보관
```

- **`server/checklist.js`** — 7대 분야별 "사진에서 무엇을 볼 것인가" 목록. **이 프로젝트에서 가장 중요한 파일**이며, 담당자가 계속 고쳐 나갑니다. 이 파일을 고치면 진단 기준이 바뀝니다.
- **`server/server.js`** — 화면 파일을 띄워 주고, 사진을 AI에 넘겨 점수표를 받습니다.
- AI는 점수뿐 아니라 **판독 근거(`observed`)와 확인 불가 항목(`notVisible`)**을 함께 돌려줍니다. "사진 → 판독 → 점수 → 시설물 → 예산"을 역추적하기 위한 것입니다.
- 출력 형식(JSON)은 스키마로 강제하므로 형식이 어긋날 수 없습니다.
- **폐쇄망 전환**: `server.js`의 `callAI()` 함수 안쪽만 로컬 sLLM 호출로 바꾸면 됩니다. **화면(HTML)은 수정 불필요.**

### 재판독 — AI 오독을 사람이 바로잡는 장치 (`/api/revise`)

AI는 사진을 잘못 읽습니다. 실제로 확인된 사례:
- 철제 **현관문**을 외부 **계단**으로 판독 → 자살 위험 점수 과대
- 노면의 `일방통행·거주자 우선주차`를 `어린이 보호구역`으로 판독

그래서 진단 결과 아래에 **"결과에 오류가 있나요?"** 칸을 두었습니다. 연구원이 잘못 본 부분을 적어 보내면 AI가 다시 판독합니다.

- **연구원의 지적이 AI의 이전 판독보다 우선**합니다 (`buildRevisePrompt`). AI가 자기 판단을 고집하면 이 기능이 무의미해지므로 지시문에 명시돼 있습니다.
- 잘못 본 대상을 근거로 매겼던 **점수까지 다시 계산**합니다.
- 지적과 무관한 분야는 건드리지 않습니다.
- 지적 내용은 `auri_revision_log`에 남아 **진단서 서식1의 "판독 보정 이력"에 인용**됩니다.

### 개선 후 이미지 생성 (4단계) — 검증 중

`/api/generate` — 원본 사진 + 확정 시설물 목록 → 개선 후 이미지 (Gemini).

- 키: `server/.env` 의 `GEMINI_API_KEY` · `OPENAI_API_KEY` (**비밀 키** — 브라우저에 노출 금지). **둘 중 하나만 있어도 됩니다.**
- 모델: 화면에서 골라 비교합니다. 서버의 `IMAGE_MODELS` 목록에 있는 것만 허용.

| 제공사 | 모델 | 호출 방식 |
|---|---|---|
| Google | `gemini-3-pro-image` | JSON (`:generateContent`) |
| OpenAI | `gpt-image-1` | 파일 업로드 (`/images/edits`) |

⚠️ **2026-09-07 담당자 지시로 두 가지만 씁니다.** 빼 둔 것: `gemini-3.1-flash-image` ·
`gemini-2.5-flash-image` · `gpt-image-1-mini`. 되살리려면 `IMAGE_MODELS` 에 한 줄씩입니다.

- **모델 추가는 `IMAGE_MODELS`에 한 줄**입니다. 화면의 선택 상자는 `GET /api/models`로 이 목록을 받아 스스로 채웁니다. **키가 없는 제공사의 모델은 목록에 아예 안 나옵니다** — 고를 수 없는 것이 보이지 않게 하려는 것입니다.
- 제공사가 달라도 **지시문과 돌려주는 모양은 같습니다**(`buildGeneratePrompt` 공용). 화면은 어느 모델인지 알 필요가 없습니다.
- OpenAI는 "만들기"가 아니라 **"고치기"(`/images/edits`)** 를 씁니다. 원본 사진을 바탕으로 편집하는 것이므로. `input_fidelity: 'high'`로 원본 보존을 강하게 겁니다 — 이 앱의 판단 기준이라서 켜 둡니다.
- ⚠️ OpenAI는 **조직 인증(Verify Organization)** 을 마쳐야 이미지 모델이 열립니다. 안 하면 403이 납니다.
- **판단 기준은 "원본 보존"입니다.** 배경 건물·간판·차선이 바뀌면 개선 전/후 문서로 쓸 수 없습니다. 지시문 절반이 "바꾸지 말라"는 내용인 이유입니다.
- 생성 결과는 `auri_after_image` 로 저장되어 **진단서 서식4**에 실립니다.
- 한 번에 잘 안 나오는 것이 정상 — 화면에서 **같은 조건으로 다시** 만들거나, **고칠 점을 글로 적어** 다시 만들 수 있습니다 (진단의 재판독과 같은 방식).
- 고칠 점을 적을 때 **바탕 이미지를 고릅니다**:
  - **원본에서 다시** — 앞선 생성에서 배경이 틀어졌을 때 안전
  - **방금 만든 이미지에서 이어서** — 대체로 마음에 들고 일부만 손볼 때. 다만 반복할수록 원본에서 멀어집니다

**★ 강조 정도 — 알파와 강화판 사이로 조정** (2026-09-14, `buildGeneratePrompt`)

| 판 | 무엇이 달랐나 | 회의 반응 |
|---|---|---|
| 알파 (08-03, `8b7cdb0`) | 눈에 띄게 하라는 말 없음 | "차이가 잘 안 보인다" |
| 강화 (09-04, `d184b46`) | `MAKE THE CHANGE CLEARLY VISIBLE` 블록 추가 | "너무 튀어 주변과 조화롭지 않다" |
| **지금** (09-14) | 강화판에서 과한 문구 4개만 낮추고 `BLEND WITH THE SCENE` 추가 | 담당자 결과 확인 대기 |

강조를 유발한 네 문구와 바꾼 방향:

| | 강화판 문구 | 지금 |
|---|---|---|
| ① 자리 | *obvious at a glance · where the viewer looks first* | 보이는 자리이되 **실제 설치 위치 우선**, 멀리 작게·화면 끝 잘림만 피함 |
| ② 크기 | *Err on the larger end of the realistic range* | **실제 크기 그대로**, 키우지 않음 |
| ③ 색 | *finishes, which are already high-contrast* (알파의 "ordinary design" 을 대체했음) | 표준 디자인·색 + **사진의 노출·채도·선명도·질감에 맞춤**, 광택·새 페인트 느낌 금지 |
| ④ 조명 | *a brighter pool of light* | 바로 아래만 **은은하게**, 번짐·후광·스포트라이트 금지 |

- **원본을 어둡게·흐리게·채도 낮춰 대비를 만드는 것은 계속 금지**입니다(그대로 유지).
- 재수정(`fromGenerated`) 지시문은 바꾸지 않았습니다 — 연구원이 적은 요청만 반영하는 용도라서.
- 결과가 다시 "안 보인다"로 기울면 ①②부터 되돌리는 것이 순서입니다(③④가 조화 문제의 주원인).

**앞으로의 갈래**: 지시형 편집(지금)이 원본을 못 지키면 → **마스크 방식(인페인팅)** 으로 전환해야 합니다. 마스크는 칠하지 않은 영역의 픽셀이 그대로 남아 구조적으로 안전하고, 폐쇄망에서 로컬 모델로 돌리기도 이쪽이 유리합니다.

**요금 안전장치** (`server.js` 상단, 환경변수로 조정)
- `DAILY_LIMIT` 하루 총 진단 횟수 (기본 200) / `HOURLY_LIMIT_PER_IP` 한 사람당 1시간 (기본 20)
- ~~`ACCESS_CODE`~~ — 2026-09-14 부터 **계정 로그인으로 대체**. 코드는 남아 있어 `AUTH_MODE=code` 로 되돌리면 다시 쓰입니다 (아래 "계정 로그인" 참고)
- 상한 확인은 **AI 호출 전에** 이뤄지므로 초과 요청에는 요금이 발생하지 않습니다.

**인터넷 배포 (데모용)**
- 서버가 화면 파일까지 서빙하므로 **배포 주소 하나로 지도·AI가 모두 동작**합니다.
- 배포 후 **네이버 콘솔 "Web 서비스 URL"에 그 주소를 반드시 등록**해야 지도가 뜹니다.
- GitHub Pages 버전은 AI 없이 화면 흐름만 보는 용도로 남습니다(서버 없으면 더미 점수).
- ~~공개 주소에 API 키가 붙는 구조이므로 상한선이 유일한 방어선~~ → 2026-09-14 부터 **로그인이 1차 방어선**, 상한선은 2차입니다.
- ⚠️ **GitHub Pages 주소는 로그인으로 막히지 않습니다.** 그쪽은 서버 없이 파일만 내주는 곳이라 문지기가 없습니다. AI는 안 되지만 화면 흐름과 `data/stats` 통계는 보입니다. 접근통제를 완전히 하려면 **저장소 Settings → Pages 를 끄는 것**을 검토하세요 (담당자 판단).

**실행 방법 (로컬)**
1. Node.js 설치 (20.6 이상) — https://nodejs.org
2. `server/.env.example`을 `.env`로 복사하고 API 키 입력 (`.env`는 .gitignore로 차단됨)
3. `cd server` → `npm install` → `npm start`
   - ⚠️ 2026-09-14 부터 `.env` 에 **`AUTH_MODE=off`** 가 없으면 내 컴퓨터에서도 "설정 필요" 화면으로 닫힙니다(DB 가 없으므로). 로그인까지 시험하려면 `DATABASE_URL` 에 렌더 **External** Database URL 을 넣으세요.
4. 브라우저에서 **http://localhost:8787** 접속 (GitHub Pages 주소가 아님)

### 119 출동자료 (실제 데이터) — 2026-08-26 최종본

AURI/강원대가 정제한 41개 지자체 3개년(2023~2025) 구급 출동자료입니다.
**한 번 읽어서 지도용 격자와 리포트용 통계를 함께** 만듭니다.

| | |
|---|---|
| 원본 | `data/raw/analysis_0909/0909_사고유형분류기준/분류 후 구굽출동 결과/0826*_유형.csv` — 0826 판 + 강원대 분류 **A_type** 칸 (2026-09-15 교체). 화재만 `data/raw/incidents_0826/0826화재_최종.csv` 와 결합. 🔴 **커밋 금지** |
| CSV 열기 | `tools/lib/incidents.mjs` — 파일 이름·화재 결합·A_type 축·반복 임계값 **한 곳** (통계·동별 HEA 공용) |
| 조사지 | `data/raw/gis_0901/최종 조사지/` — 41개 SHP (지자체당 폴리곤 1개) |
| 확정 분야 | `data/raw/analysis_0901/대표_위험_선정_기준_표본수추가.xlsx` |
| 변환 | `tools/build-stats.mjs` — `npm run build --prefix tools` |
| 점검 | `tools/verify-stats.mjs` — `npm run verify --prefix tools` |
| 결과 | `data/incidents/<지역>.json` (격자 1.9MB) · `data/stats/regions/<지역>.json` (통계 2.5MB) |

**⚠️ 옛 `tools/convert-gis.js` 는 옛 SHP(`data/raw/points/`, 34.4만 건)용으로만 남겨 두었습니다. 지금 쓰는 것은 `build-stats.mjs` 입니다.**

**정확성 검증** — 우리 계산이 AURI/강원대 값과 맞는지 295건을 대조합니다(오류 0건).
- AURI 사전진단서 [표 2] 7칸 — **소수점까지 일치**
- 강원대 표본수 표 41개 지자체 × 6개 분야 246칸 — **전부 일치**
- 자살만 제외합니다. 강원대는 구급출동 + **자살발생지점**을 합쳐 세지만(부천 1,404건) 우리는 **구급출동만**(926건) 씁니다. 자살발생지점 원본을 받지 못했고 사전진단서 3·4장도 구급출동 기준입니다. 강원대 값은 분야 선정 근거로만 참조합니다(`sampleRef`).

**원본 자료를 바로잡은 곳** (`tools/lib/regions.mjs`) — 이걸 되돌려 놔야 숫자가 맞습니다.
- **서울 중구 → 대구 중구 83건.** 주소는 "서울특별시 중구 태평로"인데 좌표는 전부 동경 128.59 / 북위 35.87 — 대구입니다. 서울·대구에 같은 동 이름이 있어 지오코딩이 잘못 붙었습니다.
- **시군구가 빈 충청남도 2건** → 좌표로 공주·당진에 배정.
- 분야는 **`안전유`(한글) 칸** 기준입니다. `분류기`(영문)는 2건이 어긋나 있어 쓰지 않습니다.

**개인정보 처리**: 원본에는 환자 연령·성별·지번주소가 들어 있습니다. 결과물은 **50m 격자 건수**와 **비율 통계**뿐이라 개별 신고를 되짚을 수 없습니다. `data/raw/`는 .gitignore로 막혀 있고, **서버도 `data/raw/`·`server/`를 403으로 차단**합니다(데모 서버가 인터넷에 올라가므로).

**격자 크기**: `build-stats.mjs` 맨 위 `CELL_SIZE`(m). 현재 **50m**.

### 통계 파일 (`data/stats/`) — 리포트 ①② 의 재료

지자체 한 곳당 파일 하나입니다. 안에 든 것:

- `focusTypes` 강원대 확정 중점 3분야 (자살은 항상 포함 — 예산 성격상 필수)
- `surveyArea` 조사지 면적·중첩 분야 수·위험도 + **경계 GeoJSON**
- `categories.<분야>` — `count`(조사지/전체/비율), `inside`·`region` 각각의 연령·성별·장소·유형·증상·원인·시간대, `compare`(배수·차이), `sample`(표본 충분한가)
- `byDong.<읍면동>` — 같은 계산을 동 단위로

**계산 규칙 (AURI 값을 역산해서 맞춘 것)**
| | |
|---|---|
| 연령 구간 | **20~49세** · 65세 이상 · 20세 이하 |
| 연령 분모 | 연령값이 있는 건수 (결측 제외) |
| 장소·유형 분모 | 그 칸이 비어 있지 않은 건수 |
| 성별 분모 | 남 + 여 |
| 장소 묶음 | 발생장은 **묶지 않습니다** — 원자료 값 그대로. 화재·범죄·자살의 장소는 2026-09-15 부터 **강원대 A_type**(교통지역 포함) |
| 동점 순서 | 건수가 같으면 **이름순** (2026-09-15 — 원본 줄 순서가 바뀌어도 목록이 흔들리지 않게) |

- AURI는 21~49세로 계산한 것으로 보이나, 그러면 10대가 11~19세가 되고 20세가 누락됩니다. 우리는 **20~49세**로 갑니다. 사전진단서와 1%p 내외 차이는 감수합니다.
- ✅ **이 방식을 유지하기로 정했습니다** (담당자, 2026-09-08). 강원대 0904 자료는 `1-9세`부터 시작해 19세까지를 20세 이하로 봅니다. 두 방식의 차이는 **오직 20세를 넣느냐**뿐이고(10년 단위 칸은 일치), 20세가 어디에도 안 들어가는 구조보다 지금이 낫습니다. 바꾸면 AURI 대조 295건을 전부 다시 검증해야 하는 것도 이유입니다.
- **표본이 30건 미만이면 "참고값"을 붙입니다.** 부천 감염병 "고령자 50.0%"는 17명입니다. 중점 3분야 중 표본이 부족한 곳이 **40개 지자체 / 83개 분야**나 됩니다.

**⚠️ 생활안전 데이터 편중** — 전국 27,682건 중 부천 16,815 · 안동 4,674 · 광주 북구 2,917 = **88%**. 제주시는 교통사고 8,028건인데 생활안전이 297건입니다. **다른 지자체에서는 분야 간 비교가 성립하지 않습니다.** AURI 파일에도 같은 값이 들어 있으니 우리 실수는 아니며, 원인 확인이 필요합니다.

**⚠️ PNUCODE 사용 불가** — 13.8만 건 전부 `5.27E+18` 지수 표기로 저장돼 유효숫자 3자리만 남았습니다. 복구 불가. 읍면동은 **`긴급_12` 칸**(법정동, 채움률 99.9%)으로 처리합니다.

**⚠️ 읍면동 건수가 사전진단서 3.2와 다릅니다** — 부천 심곡동 범죄: 우리 조사지 내 345건 vs 진단서 136건. 조사지·지자체 수준은 246칸 전부 일치하므로 우리 집계는 맞습니다. 3.2는 사람이 쓴 절이라 어떤 기준으로 셌는지 문서에 없습니다. **AURI 확인 필요.**

### 강원대 분류 A_type 반영 (2026-09-15) — `tools/lib/incidents.mjs`

0909 에 강원대가 분류 기준(`구급출동자료_행위분류.xlsx`)과 **A_type 칸이 붙은 CSV** 를 보냈습니다.
그동안 재현하지 못한 "교통지역"·"공동주택" 같은 분류의 근거입니다.

| 분야 | A_type 이 나눈 것 | 화면이 읽는 곳 |
|---|---|---|
| 화재 · 범죄 · 자살 | **장소** (공동주택·단독주택·교통지역·숙박시설 …) | 장소 = A_type |
| 생활안전 · 산재 · 감염병 | **사고유형** (낙상·기계·전신증상 …) | 유형 = A_type, 장소 = 발생장(그대로) |
| 교통사고 | **교통수단** (자동차·오토바이·차대사람 …) | 유형 = A_type, 장소 = 발생장(그대로) |

- **"교통지역" = 발생장 도로·도로외교통지역(99%) + 상세장소(`긴급__15`)의 차량·주차장·인도·정류장·골목 등.** 일부는 건축물대장·카카오맵으로 **사람이 재분류**해 규칙으로 재현할 수 없습니다 → A_type 을 그대로 씁니다.
- **"교통지역" 명칭은 강원대 분류 기준을 따라 그대로 씁니다** (담당자 결정 2026-09-15). 아래 9/7 "삭제" 결정은 발생장을 우리가 묶던 것에 대한 것이고, 강원대 분류값은 별개입니다.
- 행은 0826 판과 **줄 순서까지 같습니다**(생활안전만 순서가 다르고 내용 같음 — 순서 무관 대조). 건수·연령·성별·시간대는 바뀌지 않습니다.

**⚠️ 화재 파일만 모양이 다릅니다** — `0826화재_유형.csv` 에 A_type 칸이 없고 **원래 `type`(연기흡입·화염·화재) 칸에 장소 분류가 덮어써져** 있습니다. 0826 화재 파일과 **type 을 뺀 모든 칸이 줄마다 같을 때만** 결합합니다(3,894줄 전부 일치). 어긋나면 결합하지 않고 기존 방식 + 경고. 강원대 재전송 요청 중.

**교체 전후 검증**

| | 결과 |
|---|---|
| 통계 값 | 차이 **0** (동점 항목 순서만 달라져 이름순으로 고정) |
| AURI 대조 | verify-stats **295건 오류 0** · verify-rules 오류 0 |
| 통계 처방 규칙 (41곳×분야×조사지/전체 574경우) | 꺼진 41건 모두 재분류로 **실제 비율이 기준 아래**로 내려간 경우 — 이름 불일치 0건. 켜진 28건 |
| 현장 확인사항(장소) | 줄어든 4건은 표본 1~3건(강원대 "기타" 재분류). "기타"가 상위면 건너뛰고 다음 장소로 채움 |
| 사고유형 문장 | 379경우 바뀜 — 예) 부천 자살 조사지 "집 74.3%" → "공동주택 48.9%" |

- **`ST-PLACE-HOME`** 은 공동주택+단독주택(또는 발생장 "집") 합산.

**★ 장소 처방 임계값 재보정 (2026-09-15, `RX_PLACE_MIN`)** — 옛 기준(주거 50 · 상업 25)은 발생장 분포에 맞춘 값이었습니다.
원본 교차표: 발생장 "집" → 공동+단독 72~83% · **상업시설 9~13%**(상가주택) · 기타 등. 그래서 A_type 은 주거가 낮게, 상업시설이 높게 나옵니다.

| 규칙 | 분포 | 기준 | 근거 (41곳×82경우, 새/옛 비율 평균 · 옛 결과와 일치) |
|---|---|---|---|
| 주거 | A_type 화재·범죄·자살 | **40%** | 0.69~0.79배 · 화재 69 · 범죄 81 · 자살 71 (그대로면 64·81·59) |
| 상업 | A_type 범죄 | **30%** | 1.18배 · 74 (켜짐 72→62, 옛 64) |
| 상업 | A_type 화재·자살 | **50%** | 2.04·1.98배 · 74·78 (그대로면 63·76) |
| 둘 다 | 발생장(교통·생활·산재·감염병) | 50 / 25 그대로 | 분류 변화 없음 |

- 중점 분야 처방(41곳 246경우): 옛 발생장과 다른 규칙이 뽑힌 경우 **31건(재보정 전) → 15건(후)**.
- 미추홀 화재 조사지: 재보정 전엔 상업 35.4%가 먼저 켜져 "음식점 주방 화재안전컨설팅" → 후 **"주택용 소방시설 지원"** 복귀.
- 부천 화재 조사지는 주거 39.0% 로 40% 바로 아래라 꺼진 채입니다(부천 화재는 중점 분야가 아니라 처방에는 영향 없음).
- 근거 줄에 `(기준 40% · 강원대 분류)` 가 함께 찍힙니다.
- **`PLACE_CHECKS` 는 두 벌**(A_type 장소값 + 발생장 값)이 함께 있습니다. **발생장 열쇠를 지우면 교통·생활안전·산재·감염병의 확인사항이 사라집니다.**
- 문장·확인사항·처방이 같은 분포를 보도록 `stats.js placeTop/typeTop` 한 곳에서 고릅니다(rules.js 도 이 함수를 씀).

**A_type ↔ 강원대 0904 표** (`compare-kangwon-type.mjs`) — 범죄·교통사고·감염병·산재는 거의 전부 일치.
자살(지역 전체)·생활안전·화재(지역 전체)는 **강원대 0904 표 건수 자체가 0909 CSV 와 다릅니다**(관악 자살 84 vs 44 등) — 두 판이 다른 것으로 보이며 **강원대 확인 필요**. 우리 값은 0909 CSV 를 그대로 센 것입니다.
⚠️ 이 도구가 "대구시 중구"를 끝 단어로 찾아 **대구·대전·울산 중구가 섞이던 결함**을 고쳤습니다(`slugForKangwon`, 0908 연령 대조 도구도 함께).

**⚠️ 자살 "공동주택 62.0%, 단독주택 10.2%"(사전진단서 조사지 내)는 재현되지 않습니다.**
A_type 구급출동 조사지 내 48.9% / 10.0%(180건), 강원대 0904 표 43.6% / 8.5%(188건). 다섯 비율이 동시에 정수로 맞는
가장 작은 분모는 255건(구급 180 + 자살발생지점 약 75 추정). 자살발생지점 CSV 에 **좌표가 없어** 조사지 판정 불가 → 강원대 확인 중.

### "교통지역" 표기 삭제 (2026-09-07 AURI 확인) — ※ 2026-09-15 강원대 분류값에서는 다시 씀 (위 참고)

전에는 사전진단서를 따라 `도로` + `도로외교통지역` 을 **"교통지역"** 하나로 묶었습니다.
AURI 우 연구원이 이 이름이 **틀린 표현**이라고 확인했습니다.

> "용도지역상 '교통 지역'이라는 명칭은 없다. 잘못된 표현이다."

사전진단서 원문에 그렇게 적혀 있어도 따라가면 우리 문서까지 틀린 말을 하게 되므로,
**원자료 `발생장` 값을 그대로** 씁니다.

| | |
|---|---|
| 끄는 법 | `build-stats.mjs` 의 `PLACE_GROUPS` 를 **빈 표로** 둠 |
| 읽는 쪽 | 고칠 필요 없음 — `placeGrouped` 가 원본과 같아질 뿐입니다 |
| 함께 고친 곳 | `stats.js` 의 `PLACE_CHECKS` — `교통지역` 한 줄 → `도로`·`도로외교통지역` 두 줄 |
| 확인 | `data/stats/` 에 묶은 이름 "교통지역" **0건** · verify-stats 295건 오류 0 |

⏸ **다시 묶으려면** AURI 가 보내기로 한 **분류 기준 자료**가 도착해 올바른 상위
명칭이 정해진 뒤 `PLACE_GROUPS` 에 한 줄씩 넣으면 됩니다. 기준 없이 우리가 이름을
지어내지 않습니다.

### 강원대 산출값과 대조 (2026-09-08) — `tools/compare-kangwon.mjs`

강원대가 0904 자료로 연령·성별 비율을 **직접 산출해** 보내왔습니다
(`data/raw/인적 특성 결과_0904/`). 우리도 같은 CSV 로 같은 값을 계산해 두었으므로
부천·미추홀 기준으로 맞대어 보았습니다. `npm run compare-kangwon --prefix tools`
(`--buckets` 를 붙이면 10년 단위 칸까지 봅니다).

**결론 — 계산은 맞습니다. 차이는 두 가지 정의 차이에서만 옵니다.**

| | |
|---|---|
| 10년 단위 칸 (0-9, 10-19 …) | **사실상 완전 일치** — 표본이 충분한 곳은 0.0~0.5%p |
| `20세 이하` | 우리가 **20세를 포함**해서 1~5%p 높게 나옴 (의도한 정의 차이) |
| 분모(표본 수) | 강원대가 **일관되게 조금 적음** — 원인 미상 |

- **연령 구간 이름이 다릅니다.** 강원대는 `1-9세` 부터 시작하고 `60-64`·`65-69`
  를 따로 두며 `60대` 는 그 둘의 **소계**입니다 (총합에 두 번 세면 안 됩니다).
  파일 자체는 완전히 일관됩니다 — 구간합 = 연령총합, 41개 지자체 × 7분야 전부 검산 통과.
- **분모 차이는 설명되지 않았습니다.** 0세 제외·성별 결측·연도 필터를 모두
  검정했지만 교통사고 2칸만 "1세 이상"으로 맞고 나머지는 안 맞습니다
  (부천 생활안전 16,207 vs 우리 16,732 · 미추홀 생활안전 96 vs 141).
  → **강원대 확인 필요.** 비율 자체는 거의 같아 서술에 미치는 영향은 작습니다.

⚠️ **한셀(HCell)로 만든 엑셀이라 기존 리더가 못 읽었습니다.** 태그마다 `x:` 접두사가
붙습니다(`<x:row>`). `lib/xlsx.mjs` 에 **접두사를 떼는 한 줄**(`stripNs`)을 넣어
해결했고, 시트 이름↔파일 연결도 순서 추측이 아니라 `workbook.xml.rels` 를 따라가도록
고쳤습니다(어긋나면 감염병 자료가 교통사고 이름표를 달고 들어오는 조용한 오류였습니다).
팔레트 결과가 바뀌지 않는 것으로 기존 동작을 확인했습니다.

### 사고유형 대조 (2026-09-08) — `tools/compare-kangwon-type.mjs`

연령·성별에 이어 사고유형(`data/raw/사고유형 특성_0904/`)도 맞대어 보았습니다.
`npm run compare-kangwon-type --prefix tools`

**★ 먼저 알아야 할 것 — 분야마다 세는 축이 다릅니다.**
119 원본의 어느 칸을 쓴 것인지 값 집합을 맞대어 확인했습니다.

| 시트 | 원본 칸 | 우리와 같은 축인가 |
|---|---|---|
| **감염병** | `type` | ✅ 100% 같음 — 항목별 대조됨 |
| **교통사고** | `type` | ✅ 100% 같음 — 항목별 대조됨 |
| 화재 · 범죄 · 자살 | `발생장` | ❌ 강원대가 **재분류** (10~12개 중 2~3개만 일치) |
| 산업재해 | `질병외_` | ❌ 재분류 (18개 중 5개) |
| 생활안전 | `type` | ❌ 재분류 (15개 중 5개) |

**축이 같은 분야 — 우리 계산이 맞습니다.**

| | 강원대 | 우리 |
|---|---|---|
| 부천 지역전체 교통사고 (7개 차종) | — | 차이 **0.0~0.3%p** |
| 미추홀 지역전체 교통사고 | — | 차이 **0.0~0.5%p** |
| 감염병 (전신증상·호흡기질환) | — | 1.7~2.1%p (표본 26~251건) |

**⚠️⚠️ 조사지내 교통사고 시트는 이름과 값이 어긋나 있습니다 (자료 오류)**

지자체 이름은 **가나다순**인데 값은 **영문 지역코드 알파벳순**으로 붙었습니다.
41행 전부 밀려 있어, 이 시트를 그대로 쓰면 **모든 지자체가 남의 값을 봅니다.**

| 시트에 적힌 것 | 값 | 실제로는 |
|---|---|---|
| 부천시 | 10 | **장수군** 값 |
| 서울시 관악구 | 2,146 | **제주도** 값 |
| 강진군 | 768 | **안동시** 값 |

- 근거: 제 이름과 맞는 행 **0개** / 영문순으로 맞추면 **34개** (판별 가능한 35행 중)
- 같은 점검을 지역전체 교통사고에 돌리면 **36 대 1 로 정상**입니다 — 이 시트만의 문제입니다
- 도구가 `checkAlignment()` 로 자동 점검하고, 어긋난 시트는 **대조를 건너뜁니다**
  (모르고 대조하면 "우리가 틀렸다"는 결론이 나옵니다)
- 값이 작아 우연히 맞는 행은 판정에서 뺍니다 — 없는 문제를 있다고 말하지 않기 위해서입니다
- → **AURI 확인 필요**

**★ 자살 사고유형에 건축물 용도가 들어왔습니다** — 오래 막혀 있던 항목입니다.

우리 `발생장` 은 `집` 하나뿐이라 사전진단서의 "공동주택 62.0%" 를 쓸 수 없었는데,
강원대 자살 시트가 **공동주택 · 단독주택 · 숙박시설 · 공공기관** 으로 나눠 놓았습니다
(부천 조사지 내 공동주택 43.6% · 상업시설 18.1% · 숙박시설 10.1%).
다만 **분류 기준 문서를 받지 못해** 아직 쓰지 않습니다. 기준이 오면 이 자료로 채울 수 있습니다.

⚠️ **강원대 사고유형 자료에도 "교통지역" 이 들어 있습니다** (화재·범죄·자살 시트).
AURI 가 오류로 확인한 그 명칭입니다. 우리는 쓰지 않지만, 분류 기준 자료를 받을 때
이 부분도 함께 정리되어야 합니다.

### 물적환경 회귀분석 (`assets/physical.json`) — 강원대 자료

사전진단서 4장의 `(물적환경)` 줄은 지금까지 **사진 판독 결과만** 담았습니다.
사진은 "지금 이 자리에 무엇이 있는가"는 알려 주지만 그것이 실제로 사고와 이어지는지는
말해 주지 못합니다. 강원대 회귀분석이 그 자리를 채웁니다.

```
data/raw/물적환경_0904/       회귀분석 결과 (유의한_영향요인 시트)
data/raw/analysis_0904/DB_약어.xlsx   약어 ↔ 한글 이름
        ↓  npm run physical --prefix tools
assets/physical.json          ← 자동 생성. 사람이 손대지 않음
assets/physical.js            불러오기 · 문장 만들기
        ↓
document.html 4장  (관련요인) 줄
```

| | |
|---|---|
| 대상지 | **7곳뿐** — 부천 · 광주북구 · 공주 · 관악 · 홍천 · 남원 · 영천 |
| 유의한 요인 | 조사지 내 89건 · 지역 전체 259건 · 변수 54종 |
| 약어 해독 | 회귀분석 변수 63개를 약어표가 **빠짐없이** 덮습니다 (누락 0) |

- **회귀분석을 다시 돌리지 않습니다.** 팔레트와 같은 원칙 — 원자료를 옮겨 담기만 합니다.
  `유의한_영향요인` 시트가 이미 걸러 놓은 것을 그대로 씁니다(우리가 p값으로 다시
  거르면 두 문서가 어긋납니다).
- **⚠️ 미추홀(데모 대상지)에는 이 자료가 없습니다.** 자료가 없는 34곳에서는
  `narrate()` 가 `null` 을 돌려주고 **줄 자체가 나오지 않습니다.** 없는 것을
  "없음"이라고 적지도 않습니다.
- 조사지 내에 유의한 요인이 없으면 **지자체 전체 결과로 대신**하고, 어느 쪽인지
  문장 앞머리에 그대로 적힙니다(`조사지 내 …` / `지자체 전체 …`).
- **⚠️ 상관이지 인과가 아닙니다.** "관련성이 높은 요인"이라고만 쓰고 "때문에"라고
  쓰지 않습니다. 퍼센트 뒤에 "1 표준편차 증가 시 변화율"이라는 각주가 반드시 붙습니다 —
  이 설명이 없으면 "그 시설이 있으면 사고가 65% 는다"로 잘못 읽힙니다.

**⚠️ 한글 이름이 겹치는 약어 6쌍** — 약어표가 `ROAD_2/ROAD_2_P` 처럼 두 약어를 한 줄에
적어 두어 이름이 같아집니다. 회귀분석에는 둘이 따로 들어가고 **방향이 반대인 경우도**
있습니다(영천 교통사고 +111.8% / −53.2%). 그대로 실으면 문서가 스스로 모순되므로,
**한 목록 안에서 겹칠 때만** 약어를 괄호로 덧붙입니다. 겹치지 않으면 한글 이름만 나옵니다.
둘이 실제로 무엇이 다른지(길이 vs 비율로 짐작) **AURI 확인 필요**.

⚠️ **④대응역량은 계속 "자료 확인 중"입니다.** 약어표에 `CLINIC_den`(병원)·`BUS_den`
(버스정류장)·`SWF_den`(사회복지시설)이 있어 8/25 협의의 대응역량 항목과 겹치지만,
**목록일 뿐 실제 위치·밀도 값은 받지 못했습니다.**

### 분야별 위험 가중치 — 종합 위험지수 (`assets/risk-weights.js`)

건수를 그대로 겹쳐 보면 지도가 거짓말을 합니다. 42개 지역 3개년 실측:

| 분야 | 건수 | 비중 |
|---|---|---|
| 생활안전 | 264,287 | **76.8%** |
| 교통사고 | 53,113 | 15.4% |
| 산업재해 | 12,785 | 3.7% |
| 범죄 | 8,509 | 2.5% |
| 감염병 | 3,248 | 0.9% |
| 화재 | 1,798 | 0.5% |
| 자살 | 472 | **0.14%** |

성격이 다른 두 왜곡이 섞여 있어 **순서를 나눠서** 풉니다. 심각도 배수만 곱하면 빈도가 여전히 이깁니다(자살 ×10을 해도 472×10 < 264,287).

1. **분야 안에서 0~1 정규화** — `log(1+건수) / log(1+상위1%값)`, 1에서 자름. 빈도 차이를 없앱니다. 최대값이 아니라 상위 1%를 기준점으로 쓰는 것은 유난히 몰린 한 칸이 나머지를 0 근처로 누르지 않게 하기 위함입니다.
2. **가중치 곱하기** — 생활안전 1 기준: 자살 10 · 화재 6 · 범죄 5 · 교통 4 · 산재 3 · 감염병 2.
3. **한 칸의 7분야 합산**
4. **주변을 거리에 따라 섞음** — 칸 값을 그대로 쓰면 칸 경계에서 값이 튑니다(1m 차이로 100 → 13). 반경 188m 안의 칸을 종 모양 곡선(가우시안)으로 가중 평균해 히트맵처럼 이어 붙입니다. 반경은 `RISK_SMOOTH`(격자 칸의 배수, 현재 1.5)로 조절합니다.
5. 지역 내 상위 1%를 만점으로 0~1 환산.

**기준 분위수는 이름 있는 상수 두 개**입니다 (`assets/risk-weights.js`). 하는 일이 달라 따로 조절합니다.

| 상수 | 어디에 | 무엇을 바꾸나 |
|---|---|---|
| `RISK_DENSITY_QUANTILE` | 1단계 (분야 안 정규화) | 그 분야에서 "많이 몰렸다"의 기준 |
| `RISK_INDEX_QUANTILE` | 5단계 (최종 환산) | **지도 색과 위험 판정에 직접** 영향 |

⚠️ **새 CSV로 바꾼 뒤 분포가 크게 내려갔습니다** (2026-09-04). 분야 구성이 뒤바뀐 탓입니다(생활안전 77%→20%, 교통 15%→59%).

| | 미추홀 p50 | 안전·주의·위험 |
|---|---|---|
| 옛 데이터 (34.4만 건) | 34 | 49 · 43 · 7% |
| **새 데이터 (13.8만 건)** | **20** | **78 · 19 · 3%** |

`RISK_INDEX_QUANTILE` 을 조정하면 이렇게 움직입니다 (미추홀 p50 / 안전·주의·위험):

| 0.999 | 0.99 (현재) | 0.98 | 0.95 | 0.90 |
|---|---|---|---|---|
| 15 / 88·10·2% | **20 / 78·19·3%** | 24 / 69·25·6% | 35 / 47·37·16% | 43 / 35·40·25% |

옛 분포(p50 34)에 맞추려면 **0.95** 근처입니다. 다만 화면으로 확인한 뒤 정하는 것이 맞습니다. **분야별 가중치(`RISK_WEIGHTS`)는 AURI 확정 전까지 손대지 않습니다.**

- 가중치는 **통계가 아니라 정책적 판단**입니다. 파일 안에 분야별 근거를 함께 적어 두었습니다. 지역안전지수 산식이나 피해 통계를 받으면 교체하는 것이 맞습니다.
- 지수 3구간(0~33 / 34~66 / 67~100)은 **위험도 9단계·처방 규칙표와 같은 경계**입니다. 한쪽만 바꾸면 감사 추적이 깨집니다.
- 종합 지수는 **그 지자체 안에서의 상대 순위**입니다. 지역 간 직접 비교 불가.

### 사진 판독 + 주변 이력 = 종합 위험도

3단계 리포트와 5단계 서식1에 **세 점수를 나란히** 적습니다. 하나로 합쳐 버리면 "이 점수가 사진에서 나왔는지 통계에서 나왔는지"를 되짚을 수 없어 감사 추적이 깨지기 때문입니다.

| | 무엇을 보는가 |
|---|---|
| **사진 판독** | 지금 이 자리에 무엇이 있는가 (AI 7개 분야 평균) |
| **주변 이력** | 이 일대에서 무슨 일이 있었는가 (반경 188m·3개년) |
| **종합 위험도** | `사진 × 0.7 + 이력 × 0.3` — 비율은 `RISK_BLEND_PHOTO` |

- **사진 점수(`auri_diagnosis_results`)는 이 계산으로 바뀌지 않습니다.** 항목별 점수와 **개선 시설물 처방은 사진 판독 결과만으로** 정해집니다. 종합 점수는 표기용입니다.
- 이력이 없는 지점은 사진 점수를 그대로 씁니다 (없는 값을 0으로 넣으면 점수가 부당하게 낮아짐).
- ⚠️ **이력이 낮으면 종합 점수가 내려갑니다.** 사진 72 + 이력 1 → 종합 51. 신고가 없다는 것이 안전하다는 뜻은 아니므로(과소신고·유동인구 적음 등), 이력이 점수를 **올리기만** 하게 하려면 `auriBlendScore`를 `Math.max(photo, blended)`로 감싸면 됩니다.

실측(미추홀, 사진 72점 가정): 최고위험 지점 이력 100 → 종합 80(위험) · 중간 이력 34 → 종합 61(주의) · 최저 이력 1 → 종합 51(주의).

**지도 표시**: 7개 분야 레이어는 **가중치를 쓰지 않고** 그 분야의 원래 건수로 그립니다(분야 안에서 어디가 몰렸는지 보는 용도). 분야 간 비교와 중첩 판단은 **종합 위험도 레이어 하나**가 담당합니다. 종합 레이어는 **색을 메인 컬러 하나로 고정**하고 크기·진하기로만 분포를 보입니다 — 세 가지 변수를 동시에 쓰면 오히려 읽기 어려워서 정한 값입니다.

**⚠️ 데이터 한계**: 50m 격자에서는 칸당 건수가 대부분 1이라(미추홀 4,941칸 중 3,712칸이 단일 분야) 1단계 정규화가 거의 작동하지 않고, 종합 지수는 사실상 **"여기서 어떤 종류의 사고가 겹쳤는가"의 가중 합**입니다. 다중취약 진단이라는 과업 목적에는 오히려 맞지만, 밀도를 반영하려면 격자를 100m로 키워야 합니다(`convert-gis.js`의 `CELL_SIZE`).
**자살은 3개년 42개 지역 합계가 472건**(미추홀 8건)뿐입니다. 119 분류가 자살 사건 전체를 담지 못하는 것으로 보이며, 가중치가 가장 높은 분야가 표본이 가장 얇습니다. 이 레이어는 통계가 아니라 **개별 사례 표시**로 읽어야 합니다.

- **좌표계**: 원본 EPSG:5186(중부원점) → WGS84 변환 (proj4)
- **분류**: 원본 `분류기` 컬럼이 **우리 7대 사회재난과 정확히 일치**(LIFE/TRAFFIC/CRIME/INDUSTRIAL/INFECTIOUS/SUICIDE/FIRE)
- **지역 이름**: 파일명으로 추측하지 않고 원본 `지역_시`·`지역__1` 값에서 추출
- 지도는 선택 지점에서 **가장 가까운 지역 파일 하나만** 불러옵니다

### 공통 파일 (`assets/`) — 여러 화면이 함께 씁니다

| 파일 | 역할 |
|---|---|
| `theme.css` | 전 화면 디자인 (ATELIER-KJ 발표자료 체계). **색·여백·로고 크기는 맨 위 `:root`만 고치면 전부 반영** |
| `ui.css` | 예전 디자인. 5개 화면은 더 이상 쓰지 않고 `tools/kakao-capture-test.html`만 참조합니다 |
| `ui.js` | 상단 바(로고·단계 이동), 9단계 계산(`auriLevel`) |
| `rules.js` | **연결표 + 처방 규칙 엔진** — checklist.js와 함께 담당자가 고치는 파일. `RX_RULES.byItem`이 "체크리스트 항목 → 팔레트 사업명"을 잇습니다. 목록·단가·HEA는 여기 없고 팔레트에서 옵니다 |
| `palette.json` | **AURI 사업 팔레트 289종** — 이름·HEA·효과크기·출처·금액. `tools/build-palette.mjs`가 만들며 **사람이 손대지 않습니다** |
| `palette.js` | 팔레트 불러오기·조회. 금액 표기(`amountText`)와 출처 줄임(`sourceShort`)도 여기 |
| `stats.js` | **119 통계 → 리포트 문장** — 숫자를 문장으로 바꾸기만 합니다(계산은 build-stats.mjs가 미리 함). 리포트 ①②⑤ 담당 |
| `rx-editor.js` | 시설물 목록 표시·편집 (리포트·시각화가 공용) |
| `field-check.js` | **현장 확인사항 4범주 분류** — 체크리스트 항목 42개를 어느 범주에 넣을지. 리포트⑤와 현장용 리포트가 공용 |
| `projects.js` | **프로젝트 저장 창 · 열기** — 계정 줄의 "프로젝트 저장" 과 `projects.html` 이 공용. 저장 칸 목록은 `server/projects.js` 와 같아야 합니다 |
| `auth.js` | **로그인 계정 줄** — 이름·남은 시간·로그인 유지·로그아웃. 보여 주기만 하고, 실제로 막는 것은 서버(`server/auth.js`)입니다 |
| `doc-edit.js` | **문서 빈칸** — 편집칸 · AI 초안(`/api/opinion`) · 판단 근거 표시 |
| `site-photo.js` | **현장 사진 업로드 + EXIF** — 좌표·촬영시각 읽기. 브라우저 안에서만 처리 |
| `doc-map.js` | **문서에 싣는 지도** — 캔버스에 직접 그림. 배경은 브이월드 타일 |
| `physical.json` | **물적환경 회귀분석 결과** — 강원대 자료. 7개 지자체만. 자동 생성, 손대지 않음 |
| `physical.js` | 회귀분석 불러오기·문장 만들기. 자료 없는 지자체에서는 조용히 빠집니다 |
| `dong-hea.js` | **동별 HEA 취약도** 불러오기·상위/중위/하위·비어 있는 이유 문구. 지도(index)와 진단서 3.3 공용 |
| `risk-weights.js` | **분야별 위험 가중치 + 종합 위험지수 계산** — 담당자가 고치는 파일 |
| `logo_auri.svg` | **현재 쓰는 로고** — 건축공간연구원(AURI). 가로형 6.46:1 |
| `logo.svg` | ATELIER KJ 로고. 정사각 1:1 |

**로고 바꾸기**: `ui.js`의 `AURI_BRAND_LOGO` 한 줄 + `theme.css`의 `--logo-h`. 두 로고는 가로세로비가 크게 달라 **반드시 함께** 고쳐야 합니다.

| 주관 | `AURI_BRAND_LOGO` | `--logo-h` | 실제 가로 |
|---|---|---|---|
| AURI (지금) | `assets/logo_auri.svg` | **32px** | 207px |
| ATELIER KJ | `assets/logo.svg` | **22px** | 22px |

- AURI 로고는 `auri` 심볼 + 국문 + **영문 2단** 구성이라, 22px로 두면 영문 줄이 3px가 되어 뭉갭니다. 그래서 32px로 키우고 위아래 여백(`--brand-pad`)을 15px로 줄여 **상단 바 높이 63px는 그대로** 유지했습니다.
- 단계 표시가 아랫줄로 내려가는 지점을 900px → **1020px**로 앞당겼습니다(브랜드 묶음이 385px로 넓어짐).
- AURI 로고의 빨강 `#de2725`는 **팔레트 예외**입니다. 기관 공식 로고라 색을 바꾸지 않습니다.

### 디자인 규칙 (ATELIER-KJ 발표자료 체계)

화면을 새로 만들거나 고칠 때 아래를 따릅니다. 실제 값은 `assets/theme.css` 맨 위 `:root`에 있습니다.

| | |
|---|---|
| 색 | 메인 `#D83D64`(아주 조금만) · 대조 `#2793C9`(보조 강조) · 무채색 `#FFFFFF #CCCCCC #999999 #666666 #333333 #000000` **그 외 색은 쓰지 않음** |
| 글꼴 | Pretendard. 제목 Bold, 본문 Regular |
| 글자 크기 | **`theme.css :root`의 `--fs-*` 토큰으로만** 조절. 화면 CSS에 px을 직접 적지 않습니다 |
| 나누기 | **카드·박스 금지.** 여백과 1px `#CCCCCC` 선으로만 구분 |
| 모서리 | **각지게(0).** 단, 점·토글처럼 원래 동그란 것은 원형 유지 |
| 그림자 | 없음 |
| 강조 | 넓은 면을 메인 컬러로 칠하지 않음. 라벨·번호·핵심 하나에만 |
| 반전 | 강조가 필요한 곳(주요 버튼, 표 머리행)만 검정 바탕 + 흰 글씨 |

**예외 — 문서(`document.html`)의 표 머리행은 옅은 회색입니다** (`#F5F5F5` + 검정 글씨).
문서는 표가 많아 검정 머리행이 줄줄이 이어지면 화면에서도 인쇄물에서도 너무 강합니다.
`.doc th` 안에만 걸어 두었으므로 지도·리포트 화면의 표는 그대로입니다.

**글자 크기 (`--fs-*`)** — 실제 사용자가 공무원이라 작다는 지적을 받아 2026-08 **두 차례** 키웠습니다(본문 13.5 → 15 → 16.5). 5개 화면의 모든 크기가 이 6개 토큰을 가리키므로 **`:root` 숫자만 바꾸면 전 화면에 반영**됩니다.

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--fs-title` | 29px | 페이지 제목 (h1) |
| `--fs-h2` | 19px | 소제목 (h2), 서식 제목 |
| `--fs-lead` | 17.5px | 페이지 부제, 강조 본문 |
| `--fs-body` | 16.5px | 본문 · 버튼 · 입력창 · **표 내용** |
| `--fs-sm` | 15px | 보조 설명 · 작은 버튼 · 표 머리행 |
| `--fs-cap` | 12px | 캡션 · 각주 · 라벨 · 배지 |

줄간격은 `--lh-body`(1.7) · `--lh-tight`(1.6). **스케일 밖의 예외는 셋뿐**이며 각각 이유를 주석으로 달아 두었습니다 — h1 줄간격 1.4, 리포트 요약 숫자 39/52px, 서식 본문 줄간격 1.95(인쇄용).

**더 키울 때 먼저 터지는 곳** (FHD 기준 남은 여유):

| | 여유 |
|---|---|
| 119 레이어 **7칸 한 줄** | 22px ← 가장 먼저 터짐 |
| 예산표 우선순위 열 | 20px |
| 상단바 (1021px 화면) | 78px |

`--fs-body`를 18px 이상으로 올리면 119 레이어가 한 줄에 안 들어갑니다. 그때는 `index.html`의 `.layer-rows` 칸 수를 7 → 4로 바꿔야 합니다.

- 위험도 색: 안전 `#666666` / 주의 `#000000` / 위험 `#D83D64` — 메인 컬러가 "진짜 위험한 곳" 하나에만 쓰이도록 정한 값입니다.
- **예외 하나**: 119 레이어의 분야별 점 색은 지도에 찍히는 색과 짝을 맞춰야 해서 위 목록 밖의 색을 씁니다 (`index.html`의 `INCIDENT_CATEGORIES`).
- `theme.css`는 예전 변수 이름(`--ink --line --surface --accent --r`)도 새 값으로 연결해 둡니다. 화면별 CSS가 그대로 따라오게 하기 위한 것이니 지우지 마세요.

### 사업 팔레트 (`assets/palette.json`) — AURI 자료

**임의로 만들었던 시설물 62종을 AURI 팔레트 289종으로 교체했습니다** (2026-09-04).
원본: `7대유형_통합_HEA_성과증명_사업팔레트_0902.xlsx`

```
assets/palette.json   어떤 사업이 있는가   ← AURI 자료. 자동 생성, 손대지 않음
assets/rules.js       언제 그 사업인가     ← 우리 판단 (연결표)
```

**나눠 둔 이유**: 팔레트가 아직 확정본이 아닙니다. 새 엑셀을 받으면
`npm run palette --prefix tools` 만 돌리면 되고, 사업명이 바뀌어 연결이
끊어진 곳은 `npm run verify-rules --prefix tools` 가 잡아 줍니다.

| | |
|---|---|
| 사업 수 | 289종 (교통 46 · 화재 44 · 자살 44 · 감염병 44 · 범죄 38 · 생활안전 38 · 산재 35) |
| HEA | E 119 · A 105 · H 65 |
| 효과크기 채움률 | **98%** / 출처 **95%** |
| 연결표가 실제로 쓰는 것 | 83종 (나머지는 연구원이 드롭다운에서 고를 수 있는 범위) |

**엑셀 읽는 규칙**
- 시트마다 표가 **두 덩어리** 쌓여 있고 위쪽만 셀에 배경색이 있습니다.
  **칠해진 쪽(위) = 기존 팔레트 · 안 칠해진 쪽(아래) = 신규 팔레트.** 같은 사업명은 신규를 남깁니다(145건 정리).
  - 근거는 AURI 우제헌 연구원 메일: *"색이 있는 부분은 기존 사업팔레트이며, 그 아래 색이 없는 부분은 비중을 차등화하여 새롭게 정리한 팔레트입니다."*
  - 신규 팔레트는 HEA를 다시 매긴 것이 확인됩니다 — 단속·점검이 E→A로(불법주정차 무인단속, 어린이 놀이시설 안전점검), 지원사업이 E→H로(주택용 소방시설 지원, 취약 어르신 낙상방지 안심홈) 옮겨졌습니다. "비중 차등화"가 이것입니다.
  - 뒤집어야 하면 `build-palette.mjs`의 `COLORED_IS_NEW` 한 줄만 바꾸면 됩니다.
- 산업재해 시트는 `구분` 열이 하나 더 있어 **머리글 이름으로** 열을 찾습니다.
- HEA 칸에도 색이 칠해져 있지만 A가 5가지 색으로 나와 **일관성이 없어 무시**합니다. 글자(H/E/A)만 씁니다.
- 요약 시트는 해설이라 건너뜁니다. (요약의 596건은 정리 전 모음집 기준이라 상세 시트 합계와 다릅니다)

**HEA = 8/25 협의의 3분류**

| | | 이미지 |
|---|---|---|
| **H** 피해대상 보호·지원 | 검진·접종·용품 지원·상담 | 그릴 수 없음 |
| **E** 환경적 개입 | 시설 설치·정비 | **그림** |
| **A** 행위 개입 | 단속·교육·홍보·제도 | 그릴 수 없음 |

- **개선 후 이미지에는 E만 들어갑니다.** 나머지는 거리 사진에 나타나지 않아 넣으면 모델이 없는 시설을 지어냅니다. 목록·예산·문서에는 그대로 남고, 화면에 왜 빠졌는지 적힙니다.
- **자살은 E만으로 채우지 않습니다** (`RX_REQUIRE_HEA`). H 또는 A가 최소 1개 들어가며, 근거 줄에 `사람 중심 개입 필수 포함(2026-08-25 협의)`이 찍힙니다. 처방이 1종뿐인 '주의'에는 적용하지 않습니다 — 그 자리를 바꾸면 물리적 대책이 통째로 빠집니다.

**⚠️ 연결이 약한 항목 4개** (`RX_WEAK`) — 팔레트에 그 문제를 직접 다루는 사업이 아직 없습니다. AURI가 생활안전·감염병의 H·A를 재정리할 때 채울 자리입니다.

| 항목 | 무엇이 없는가 |
|---|---|
| `INF-3` | 옥외 손 위생 시설(세면대·손소독제) |
| `INF-5` | 보행로 폭·밀접 접촉 |
| `LIF-6` | 보행자와 자전거·이륜차 동선 분리 |
| `SUI-3` | 고지대·경사지 추락방지 (교량 기준만 있음) |

<details>
<summary>이전 방식 (2026-09-04에 대체됨)</summary>

### 개입 유형 3분류 + 비시설 사업 (2026-08-25 행안부 협의)

행안부: *"단순 시설 설치나 기존 사업 나열이 아니라, 실제 사고·피해 감소에 직접
기여하는 사업으로 재정리하라."* → 처방 규칙표가 **시설물뿐 아니라 비시설 사업도**
담습니다.

| 유형 | 무엇인가 | 예 |
|---|---|---|
| **인적 개입** (`human`) | 사전 발견, 안부확인, 교육, 상담, 복지·보건 연계 | 고립위험 주민 안부확인, 찾아가는 마음건강 상담 |
| **환경적 개입** (`environment`) | 사고·피해가 커지기 쉬운 공간·시설 개선 | 보안등, 보도 확폭, 핸드레일 |
| **직접요인 저감** (`direct`) | 사고원인·피해확대 요인을 직접 제거·차단 | 옥상 자동개폐장치, 방호울타리, 적치물 정비 |

- 분류표는 `RX_INTERVENTION` — **`RX_PRICES`와 같은 방식(이름 → 값)**이라 규칙표 500줄을 건드릴 필요가 없습니다.
- 화면과 **서식2 예산표에 개입 유형 열**이 함께 나옵니다.
- 비시설 사업 9종 추가 (53 → **62종**): 자살 3 · 범죄 1 · 생활안전 1 · 산재 1 · 감염병 2 · 화재 1.

**⚠️ 자살은 인적 개입이 반드시 1개 들어갑니다** (`RX_REQUIRE_KIND`).
협의 원문: *"옥상 개폐장치, CCTV, 펜스 등 단순 시설사업은 고립·우울·사회적 단절 등
근본 위험요인을 직접 줄이는 데 한계가 있다."*
- 적합도 순서를 뒤엎지 않고, 상위 N개 안에 인적 개입이 없을 때만 가장 적합도 높은 인적 개입을 끌어올립니다. 근거 줄에 `인적 개입 필수 포함(2026-08-25 협의)`이 찍힙니다.
- **처방이 1종뿐인 '주의' 판정에는 적용하지 않습니다.** 취지가 "시설과 사람 개입을 묶어 패키지로" 인데, 한 자리뿐일 때 바꿔 버리면 물리적 대책이 통째로 빠집니다.

**인적 개입은 개선 후 이미지에서 제외됩니다** — 안부확인·상담·교육은 거리 사진에
나타나지 않습니다. 넣으면 모델이 없는 시설을 지어냅니다. **처방 목록·예산·진단서에는
그대로 남고**, 화면에 왜 빠졌는지 적힙니다. (`auriRxIsVisual`)

</details>

### 문서 2종으로 분리 (2026-09-07 AURI 화상회의)

5단계 `document.html` 이 **탭 두 개**로 나뉩니다. 하나였을 때는 AURI 양식에 없는
칸이 섞여 들어가 "이게 사전진단서인지 우리 리포트인지" 알 수 없었습니다.

| 탭 | 무엇 | 누가 읽는가 |
|---|---|---|
| **현장용 리포트** (첫 탭) | 진단 위치 · 현장 확인사항 · 개선 전/후 · 사업 목록 | 현장 나가는 실무자 |
| **사전진단서** | AURI 양식 그대로 — 표지 · 1~5장 | AURI·행안부 제출용 |

- ★ **순서 — 현장용 리포트 → 사전진단서** (2026-09-14 AURI 회의). *"현장에 나가기 전 예비 확인을
  먼저 하고, 사전진단서가 그다음 결과로 나오는 순서가 맞다."* 탭 순서와 처음 열리는 탭이 모두
  현장용 리포트입니다. 안쪽 이름(`docA`=사전진단서, `docB`=현장용)은 그대로 두고 보이는 순서만 바꿨습니다.

- **사전진단서에는 AURI 원문에 없는 요소를 넣지 않습니다.** 우리가 계산한 종합 위험지수,
  적합도 점수, 규칙 번호 같은 것은 현장용 리포트로 갑니다.
- **AURI 숫자는 계속 바뀝니다.** 그래서 맞춰야 하는 것은 값이 아니라 **구조와 서식**입니다.
- 각각 따로 인쇄됩니다 — 인쇄할 때 보이지 않는 탭은 통째로 감춥니다.

### 문서 빈칸 — 편집칸 + AI 초안 (`assets/doc-edit.js` · `/api/opinion`)

전에는 "여기는 사람이 채우는 자리입니다"라는 **고정 안내문**이었습니다. 웹에서 고칠 수도
없고 인쇄하면 그 안내문이 보고서에 그대로 실렸습니다. 이제 **빈 입력칸**입니다.

- 안내문은 placeholder 로 들어가 **화면에만** 보입니다.
- **AI 초안 버튼**을 눌렀을 때만 글이 생깁니다. 평소에는 비어 있습니다.
- 사람이 **한 글자라도 고치면 AI 표시가 사라집니다** — 그때부터는 사람이 쓴 글입니다.
- AI 표시는 **화면에만** 보이고 인쇄에는 안 나옵니다. 최종 문서에 "AI가 썼다"고 적히면
  행정 문서로 쓰기 어렵고, 어차피 사람이 검토해 확정한 글이기 때문입니다.
- 저장은 `auri_doc_notes`.

**인쇄는 입력칸이 아니라 사본을 찍습니다** (2026-09-07)

입력칸(textarea)을 그대로 인쇄하면 **글이 길 때 칸 안에서 잘리고**, 서체·크기가
본문과 달라 이질적으로 보입니다. 그래서 같은 글을 담은 `.doc-fill-print` 사본을
옆에 두고, 인쇄할 때 입력칸을 감추고 사본을 보입니다. 사본은 그냥 글이라 잘릴 일이
없고 본문 서식을 그대로 받습니다.

| | 화면 | 인쇄 |
|---|---|---|
| 입력칸 | 보임 | 감춤 (사본으로 대체) |
| AI 초안 표시 | 보임 | **감춤** — 행정 문서에 "AI가 썼다"고 적히면 안 됨 |
| **판단 근거** | 보임 | **보임** — 무엇에 기대어 쓴 글인지 종이에 남아야 함 |
| 대조 전 근거 안내 | 보임 | 감춤 — 우리 시스템 상태이지 문서의 근거가 아님 |

화면에서는 글 길이에 맞춰 칸 높이가 늘어납니다(`autoGrow`). 감춰진 탭에서는
높이를 잴 수 없어 건너뛰고, 탭을 열 때 다시 맞춥니다.

**★ 법령을 지어내지 못하게 막는 방법 (`server/legal.js`)**

AI에게 "관련 법령을 근거로 들어라"고만 하면 없는 조항을 지어냅니다. 그러면 행정 문서로
쓸 수 없습니다. 그래서 **조문 원문을 프롬프트에 미리 넣고, 그 안에서만 인용하게** 합니다.

```
server/legal.js  →  legalBlock()  →  프롬프트에 붙임  →  AI는 이 목록 밖을 인용 못 함
```

- 근거를 못 찾으면 **"관련 근거 확인 필요"** 로 두게 지시합니다. 억지로 붙이는 것보다 낫습니다.
- 조문을 늘리려면 `LEGAL_BASIS` 배열에 한 덩어리 추가하면 됩니다. **원문을 그대로** 넣으세요.

**★ 대조를 마친 것(`verified: true`)만 지시문에 들어갑니다** (2026-09-07)

대조 전인 것을 넣고 화면에 경고만 띄우면 실무자가 그대로 인용해 행정 문서에 실립니다.
그래서 **AI에게 아예 보여 주지 않습니다.**

| | |
|---|---|
| `legalBlock()` | `verified: true` 만 지시문에 넣음. 하나도 없으면 "법령을 언급하지 말라"고 지시 |
| `PENDING` | 대조 전이라 **쓰지 않은** 근거. 화면에만 "인용하지 않았음"으로 표시 |

**★ 근거는 세 유형이고 인용 방식이 다릅니다** (`kind` · `BASIS_KINDS`)

성격이 다른 것을 같은 형식으로 인용하면 문서가 틀린 말을 합니다.
특히 국정과제를 "○○법 제△조에 따라"로 쓰면 안 됩니다.

| `kind` | 표기 | 무엇 | 인용 범위 |
|---|---|---|---|
| `law` | **법령** | 법률 조문 | **조문 번호와 제목까지만.** 본문 전문은 인용하지 않음 |
| `policy` | **정책** | 국정과제 등 | "…에 따른 사업 방향" 꼴. 법령 형식 금지 |
| `consult` | **협의** | 회의록·검토 의견 | 요지를 풀어 써도 됨 |

**본문 전문을 인용하지 않는 이유**: 조문은 개정으로 자구가 바뀝니다. 우리가 옮겨 적은 문구가
현행과 한 글자라도 다르면 그대로 오류가 됩니다. 조문 번호와 제목은 안정적이고, 그것만으로
근거 제시가 됩니다. 그래서 `gist`(요지)는 **사람이 읽는 메모**일 뿐 지시문에 넣지 않습니다 —
협의 문서만 예외입니다(원문이 우리 손에 있어 요지를 책임질 수 있음).

각 항목의 `cite` 문구를 **글자 그대로** 쓰게 합니다.

| ID | 유형 | 인용 문구 | 상태 |
|---|---|---|---|
| `DSMA-66-13` | 법령 | 재난 및 안전관리 기본법 제66조의13(안전사업지구의 지정 및 지원, **2025.10.1. 시행 기준**) | ✅ 조문 번호·제목 확인 |
| `DSMA-PURPOSE` | 법령 | 재난 및 안전관리 기본법 제1조(목적) | ✅ 조문 번호 확인 |
| `POLICY-72-73` | 정책 | 국정과제 72·73(지역안전지수 고도화 및 안전취약지역 관리) | ⏸ **원문 확인 중** |
| `MOIS-2026-08-25` | 협의 | 행정안전부 협의(2026-08-25) 「개선사업 재정리 방향」 | ✅ 회의록 원문 확인 |
| `MOIS-REPORT-FORM` | 협의 | 행정안전부 「데이터 분석 결과 보고서」 보완사항 검토 | ✅ 원문 PDF 확인 |
| `ORDINANCE-LOCAL` | 조례 | 해당 지자체 재난안전 관련 조례 | ⏸ **지자체별 확인 필요** |

**★ 조례는 네 번째 유형(`ordinance`)으로 넣되 인용하지 않습니다** (2026-09-07 AURI 요청)

AURI 가 "해당 지자체 재난안전 관련 조례(예: 부천시)"를 근거로 넣자고 요청했습니다.
다만 조례는 **41개 지자체가 각각 다르고** 이름·조 번호·현행 여부를 하나도 확인하지
못했습니다. 확인 안 된 조례를 AI 에게 보여 주면 "부천시 안전도시 조성 조례 제5조" 같은
그럴듯한 문장을 만들어 냅니다.

| | |
|---|---|
| 지금 | `verified: false` → **지시문에서 제외**. 화면에만 "인용하지 않은 근거"로 표시 |
| AI 지시문 | 조례가 필요해 보이면 본문에 쓰지 말고 `확인필요` 로 적으라고 명시 |
| 나중에 | 지자체별로 확인해 `ORDINANCE_BY_REGION` 에 넣고 `verified: true` 로 |

→ 결과적으로 지금 AI 가 인용할 수 있는 것은 **법령 2건 + 협의 2건**뿐입니다.

**⚠️ 제66조의13 은 최근 개정으로 이동한 조항입니다.** 시점을 반드시 함께 적습니다 —
개정 전 자료를 본 사람은 **제66조의12** 로 알고 있습니다.

| | |
|---|---|
| 2022년 기준 | 제66조의12 (안전사업지구의 지정 및 지원) |
| 법률 제20653호 개정 | 종전 66조의13·14 가 각각 14·15 로 밀림 |
| **2025.10.1. 시행** (법률 제21065호) | **제66조의13** ← 현행 |

⚠️ 두 법령 모두 **조문 본문 전문은 아직 대조하지 않았습니다.** 번호와 제목만 확인된
상태라 본문 인용을 막아 둔 것이며, 이 상태로도 근거 제시는 됩니다.

### 현장 확인사항 4범주 (`assets/field-check.js`)

나열식이던 ⑤ 현장확인 사항을 범주로 묶었습니다. 현장에서 **볼 곳이 서로 다르기** 때문입니다.

| 범주 | 무엇 | 항목 수 |
|---|---|---|
| **물리적 공간 특성** | 폭·경사·시야·동선 등 공간의 생김새 | 15 |
| **시설·설비** | 조명·CCTV·난간·소화전 등 설치물의 유무와 상태 | 15 |
| **운영·관리** | 적치물·불법주정차·방치 등 치우고 단속할 것 | 12 |
| **이용자 특성 관련 확인** | 연령대·시간대 | 119 출동자료에서 채움 |

**★ 네 범주 모두 "현장에서 할 행동"으로 적습니다.**

`고령자 비중이 높음` 같은 통계 배경 서술은 현장에서 볼 수 없어 쓰지 않습니다.
**무엇이 많다 — 무엇을 확인** 꼴로 씁니다.

| | |
|---|---|
| ✗ 배경 서술 | 고령자 비중이 높음 |
| ✓ 확인 행동 | 고령자 이용 많음 — 보행 손잡이, 경사로 기울기, 휴식 벤치, 야간 조도 확인 |

- 체크리스트 42개 전부에 범주가 붙어 있습니다. 새 항목을 넣으면 **여기 표에도** 넣어야
  합니다 — 안 넣으면 어느 범주에도 안 들어가 화면에서 사라집니다.
- 이용자 특성만 사진이 아니라 통계에서 나옵니다. **중점 3분야에만** 붙습니다.
  문장은 `assets/stats.js` 의 `AGE_CHECKS` · `HOUR_CHECKS` · `PLACE_CHECKS` 세 표에 있습니다.
- **어느 범주로 갈지는 `stats.js` 가 문장과 함께 붙여 보냅니다**(`{text, group}`).
  전에는 받는 쪽에서 문장을 정규식으로 훑어 추측했는데, 문구를 고칠 때마다 조용히
  어긋나서 만드는 자리에서 붙이도록 바꿨습니다.
- 리포트(`report.html` ⑤)와 현장용 리포트가 **같은 함수**를 씁니다. 따로 두면 두 문서가
  어긋납니다.

### 현장 사진 업로드 (`assets/site-photo.js`)

사전진단서 4장은 **왼쪽 지도 · 오른쪽 사진**입니다. 여러 장 올릴 수 있습니다.

- **EXIF에서 GPS 좌표와 촬영시각을 읽어** 지도에 자동으로 찍습니다.
- ⚠️ **카카오톡으로 주고받은 사진은 EXIF가 지워집니다.** 그럴 때는
  "지도에서 위치를 지정해 주세요"로 안내하고, 지정한 좌표를 붙입니다(캡션에 `(지도에서 지정)` 표시).
- **사진을 서버로 보내지 않습니다.** 위치와 사람이 찍히므로 보낼 이유가 없으면 안 보냅니다.
  ⚠️ **예외 (2026-09-15)** — **프로젝트 저장**에서 "현장 사진 함께 저장"을 켜면(기본 켜짐) 사진이 서버 DB 에 저장됩니다. 저장 창에 그 사실을 적어 두었고, 끄면 사진만 빼고 저장합니다.
  EXIF 읽기도 브라우저 안에서 합니다(라이브러리 없이 APP1 구역만 훑음).
- 긴 변 1400px로 줄여 담습니다 — 원본 그대로면 sessionStorage 한도를 넘습니다.
- **어느 절에 실을지 고릅니다** (`field`). 여러 장을 올리면 사진이 가장 적은 절부터
  하나씩 자동 배정되고(세 장이면 4.1·4.2·4.3), 드롭다운으로 바꿀 수 있습니다.
  한 절에 두 장 이상이면 첫 장이 크게, 나머지는 아래에 작게 붙습니다.
- 좌표가 없는 사진은 **지도에서 지정** 버튼으로 찍습니다. 아래 지도와 같은 방식이라
  누른 자리를 그대로 위경도로 되돌립니다.

### 문서에 싣는 지도 (`assets/doc-map.js`) — 2026-09-07

사전진단서 3.1(중점대상지 종합위험도)과 4장(밀집구간별)에 지도가 들어갑니다.
**지도 화면을 캡처하는 것이 아니라 캔버스에 직접 그려 그림으로 넣습니다.**

**★ 왜 네이버 지도를 캡처하지 않는가**

캡처할 수 없기 때문입니다. 타일이 다른 서버(pstatic.net)에서 오는데 그 서버가
"가져다 써도 된다"는 표시(CORS)를 붙여 주지 않아 **캔버스가 오염되어 저장이 막힙니다.**
스트리트뷰 캡처가 막힌 것과 같은 이유입니다(7절). **html2canvas 를 써도 같은 곳에서
막힙니다** — 그 도구도 결국 같은 캔버스에 그려 넣기 때문입니다.

**그래서 브이월드입니다** (국토교통부 공공 서비스, 2026-09-07 확인)

```
https://xdworld.vworld.kr/2d/Base/service/{z}/{x}/{y}.png
→ Access-Control-Allow-Origin: *   ★ 이것 때문에 됩니다
→ 인증키 불필요 · 256×256 PNG
```

폐쇄망으로 갈 때도 이쪽이 낫습니다. 필요한 구간의 타일만 미리 받아 두면 되고,
공공 자료라 반출 문제가 없습니다.

| | |
|---|---|
| 3.1 | 1000×620 · 조사지 경계(붉은 점선) + 7개 분야 출동 지점 + 진단 지점 |
| 4.x | 760×570 · 진단 지점 반경 **450m**(`MAP_RADIUS_M`) + 그 분야 지점만 |
| 분야 색 | `MAP_COLORS` — `index.html` 의 `INCIDENT_CATEGORIES` 와 **같은 값** |
| 함께 그림 | 축척 막대 · 출처 문구 |

- **배경 없이도 됩니다.** `base: false` 를 넘기면 타일을 받지 않고 경계선과 점만
  그립니다. 망이 끊겨도, 타일을 못 받아도 지도가 나옵니다(타일 한 장이 실패해도
  그 자리만 비고 나머지는 그려집니다).
- 확대 단계는 범위에 맞춰 자동으로 고릅니다(`fitZoom`). 여백 비율(`pad`)이 크면
  한 단계 멀어져 거리가 안 보이므로 4장은 5%로 좁혀 두었습니다.
- 한 번 그린 지도는 `_mapCache` 에 남아 다시 그리지 않습니다. 사진을 올려 문서를
  다시 그려도 타일을 또 받지 않습니다.
- 문서를 먼저 띄우고 지도는 **뒤따라 채웁니다** — 타일을 기다리느라 문서가 늦게
  뜨면 안 되기 때문입니다.

### 개선 전/후 이미지 — 상하 배열 + 필수 문구

좌우로 놓으니 작아서 안 보인다는 의견이라 **위아래로** 바꾸고 크기를 키웠습니다.

**★ 개선 전/후 이미지 아래에는 반드시 이 문구가 붙습니다** (2026-09-14 상시 표기로 강화):

> AI가 생성한 참고용 예시 이미지입니다. 시설물의 종류·위치·규격은 관계 법령 및 설치기준에 따른 전문가 검토가 필요합니다.

| 자리 | 언제 보이나 |
|---|---|
| `visualize.html` 개선 후 | 생성 중·실패·완료 **언제나** (전에는 완료 뒤에만) |
| `document.html` 현장용 리포트 개선 전/후 | 이미지가 아직 없어도 **항상** · 인쇄물에도 |

- 문구는 **`assets/ui.js` 의 `AURI_AI_DISCLAIMER` 한 곳**에만 있습니다. 화면마다 따로 적으면 서로 달라집니다.
- 사전진단서 탭에는 개선 전/후 이미지가 없어서(AURI 양식) 문구도 없습니다. 이미지를 넣게 되면 같은 상수를 쓰세요.
- 배경: 자문회의에서 교통 전문가가 **회전교차로 사진에 횡단보도가 그려진 것**을 지적(회전교차로에는 보통 두지 않음).
  생성 모델은 법규를 모르므로 법령에 맞지 않는 배치가 나올 수 있습니다. 지우지 마세요.

### HEA 색 구분 (`assets/theme.css`)

세 유형이 서로 구분되게 각각 다른 색을 줍니다. 진단서 부록은 **행 배경을 아주 옅게** 깔았습니다.

| | 글자색 | 부록 행 배경 |
|---|---|---|
| **[E]** 환경 개선 | `--g3` (짙은 회색) | `rgba(102,102,102,.045)` |
| **[A]** 행위 개입 | `--contrast` (파랑) | `rgba(39,147,201,.055)` |
| **[H]** 피해대상 보호·지원 | `--main` (메인) | `rgba(216,61,100,.05)` |

- 배경은 **거의 안 보일 만큼 옅게** 둡니다. 카드·박스를 쓰지 않는 디자인 규칙과 부딪치지 않게,
  칠하는 것이 아니라 "줄이 어렴풋이 묶여 보이는" 정도입니다.
- 인쇄에서도 남도록 `print-color-adjust:exact` 를 걸었습니다.

### 금액 — ⚠️ 팔레트에 20%만 적혀 있습니다

**우리가 단가표를 만들지 않습니다.** 금액은 AURI 팔레트의 `금액` 칸에 적힌 문구가 전부이고, 그중 **단위와 금액이 분명한 것만** 계산에 씁니다.

| | 종수 | 어떻게 표시되나 |
|---|---|---|
| 단가로 쓸 수 있음 | **15종** | 금액 계산 · 합계에 포함 |
| 금액 문구는 있으나 단가 아님 | 50종 | `산출 근거 필요 — 참고: <원문>` · 합계 제외 |
| 금액 없음 | 224종 | `산출 근거 필요` · 합계 제외 |

**왜 대부분 못 쓰는가** — 문구의 기준이 제각각이라 일괄로 숫자를 뽑으면 문서가 거짓말을 합니다.

- `개소당 2,500만원` → 씁니다 (단위 + 단일 금액)
- `안산시 노후관로 1,386km 대상 2,229억` → 그 지자체 총액. 격자 단가가 아님
- `1개소당 5~10억 이내` → 범위. 어느 쪽인지 사람이 정해야 함
- `삼척시 기준 국비 30억원 + 지방비 20억원` → 분담 방식이 문서마다 다름

제외 사유는 `amount.reason`에 남고, 서식2 아래에 참고 문구를 모아 보여 줍니다. 판정 규칙은 `tools/build-palette.mjs`의 `readAmount()` 한 곳에 있습니다.

⚠️ 그래서 **서식2 합계가 대부분 "산출 불가"로 나옵니다.** 예산 근거 문서에 근거 없는 숫자를 올리지 않으려는 것이며, AURI가 단가를 채워 주면 저절로 계산됩니다.

### 위험도 9단계

화면에는 **9단계**로 세밀하게 보여 주고, 처방 규칙표는 **3구간**을 씁니다.
경계가 맞물려 있어야 감사 추적이 유지됩니다 — 한쪽만 바꾸면 안 됩니다.

| 9단계 | 3구간 | 점수 | 처방 |
|---|---|---|---|
| 1~3 | 안전 | 0~33 | 없음 |
| 4~6 | 주의 | 34~66 | 권장 |
| 7~9 | 위험 | 67~100 | 필수 |

### 연구원 보정 (감사 추적의 핵심)

AI 결과는 **기본값**이고 연구원이 고칠 수 있습니다. 고친 내용은 **덮어쓰지 않고 따로 보관**해서, 점수를 다시 조정해도 살아남고 "규칙이 뽑은 것 / 사람이 손댄 것"이 문서에 구분되어 남습니다.

- 점수 보정 → `edited: true` → 화면·문서에 "연구원 보정" 표시
- 시설물 수정·삭제·추가 → `auri_rx_overrides` 에 저장 → 규칙 엔진 결과와 병합

**sessionStorage 키**
- `auri_picked_location` — 선택 지점 `{addr, lat, lng, risk?}`
  ※ `risk`는 그 자리의 119 이력 종합 위험지수 `{score, level, radius, top:[{key, name, share}]}`. 이력이 없으면 `null`. 3단계 리포트와 5단계 서식1이 읽습니다.
- `auri_input_image` — 진단 입력 이미지 `{dataUrl, source}`
- `auri_diagnosis_results` — 분야별 점수 `[{key, name, score, level, findings?, observed?, notVisible?, confidence?, edited?}]`
  ※ `findings`는 항목별 판독 `[{id, ask, answer, note, risk}]`. **처방은 이 값으로 시설물을 고릅니다.** `findings` 이하는 AI 진단일 때만 있습니다(더미 점수에는 없음).
- `auri_revision_log` — AI 오독 지적 기록 `[{at, note}]` (진단서 서식1에 인용)
- `auri_rx_overrides` — 연구원이 손댄 내용 `{removed:[규칙번호], edits:{규칙번호:{...}}, added:[...]}`
- `auri_after_image` — 개선 후 이미지 (생성 모델 연결 시 저장, 진단서 서식4에서 사용)
- `auri_doc_notes` — 문서 빈칸에 쓴 글 `{칸이름:{text, ai, basis}}` (`ai`는 AI 초안 여부, 사람이 한 글자라도 고치면 false)
- `auri_project` — 지금 이 탭이 어느 저장 프로젝트에서 왔는지 `{id, title, mine, ownerName, lat, lng}`. 저장할 때 덮어쓰기 여부를 정합니다. **서버에는 저장하지 않습니다**(프로젝트 내용이 아니라 탭의 상태)
- `auri_last_user` — 이 탭에서 마지막으로 로그인한 아이디. **다른 계정으로 로그인하면 이 탭의 `auri_*` 자료를 모두 지웁니다** (앞사람 진단 결과가 보이지 않게). 로그아웃할 때도 지웁니다
- `auri_site_photos` — 현장 사진 `[{id, name, dataUrl, lat, lng, takenAt, note, manual?, field?}]` (사전진단서 4장)
  ※ `field`는 4장의 어느 절에 실을지(분야 열쇠). `manual`은 좌표를 지도에서 찍었다는 표시.
- `auri_region_stats` — 이 지점이 속한 지자체 통계 요약 `{region, label, shortLabel, inside, dong, focusTypes}` (3단계가 저장)
- `auri_prescriptions` — 최종 사업 목록 `[{id, name, note, category, score, levelLabel, kind, required?, priority, trigger?, edited?, custom?}]`
  ※ `kind`는 개입 유형(`human`/`environment`/`direct`), `required`는 협의로 반드시 넣은 유형.
  ※ `trigger`는 이 시설물을 부른 체크리스트 항목 `{id, ask, note}`. 예비 목록에서 나온 항목은 `null`.
  ※ 규칙 엔진(`auriPrescribe`)은 3단계에서 **한 번만** 돌리고 4·5단계는 그 결과를 그대로 받습니다. 단계마다 다시 계산하면 처방이 달라져 감사 추적이 깨집니다.

**다음 작업 (우선순위 순, 2026-09-08 기준 — 9/14 초안 · 9/17 중간보고)**

0. ★★ **동별 HEA 실물 확인** (2026-09-15) — 부천 지점에서 지도 "동별 HEA 취약도" H/E/A/종합 전환, 동 경계선·점수 숫자가 겹치지 않는지, 동을 누르면 근거가 나오는지. 사전진단서 3.3 [표 2-1] 인쇄 미리보기. 미추홀(E 자료 없음)에서 E 버튼이 잠기는지
0. ★★ **A_type 문장 확인** — 리포트 ②·사전진단서 4장 사고유형 문장이 "공동주택 48.9%"처럼 바뀐 것, 부천 화재에서 "주거공간" 처방이 빠진 것이 의도에 맞는지
0. ★★ **프로젝트 저장 실물 확인** (2026-09-15) — 진단 1건 → 계정 줄 "프로젝트 저장" → 탭 닫기 → 다시 로그인 → "내 프로젝트"에서 열기 → 리포트·개선 후 이미지·문서 빈칸 글이 그대로인지. 관리자로 "전체 사용자" 탭에서 남의 프로젝트가 **열람**으로 열리는지
0. ★★ **렌더 DB 설정 → 로그인 실물 확인** (2026-09-14, 9/15 필요) — 아래 "계정 로그인" 의 설정 절차. 설정 전에 코드가 배포되면 사이트가 "설정 필요" 화면으로 **닫혀 있습니다**(의도한 동작).
   - 관리자로 로그인 → 시험 계정 가입 신청 → 승인 → 그 계정으로 진단 1회 → 계정 관리 화면의 **진단 실행 이력**에 좌표·지자체가 찍히는지
   - 개선 이미지 톤(강조 조정) 결과 확인
0. ★ **브라우저 실물 확인 (가장 급함)** — 아직 한 번도 화면으로 못 봤습니다.
   서버를 켜고(`cd server && npm start` → http://localhost:8787) 두 지점을 봐야 합니다.
   - **부천** — 물적환경 `(관련요인)` 줄이 4장에 나오는지. 회귀분석이 있는 7곳 중 하나입니다
   - **미추홀** (위도 37.4596, 경도 126.6799) — 그 줄이 **안 나오는지**. 자료가 없는 34곳의 모습입니다
   - 인쇄 미리보기에서 지도·사진·빈칸 사본이 남는지
0-1. **`server/legal.js` — `POLICY-72-73` 원문 대조** — 국정과제 이행계획서 확인 중(담당자). 그때까지 AI 의견에서 빠져 있습니다. 법령 2건은 조문 번호·제목만 확인된 상태라 **본문 인용을 막아 두었습니다.** 조례는 지자체별 확인 전까지 함께 제외돼 있습니다.
0-2. ✅ **사고유형 0904 대조 완료** (2026-09-08) — 축이 같은 감염병·교통사고는 0.0~0.5%p 로 일치. **조사지내 교통사고 시트는 이름과 값이 어긋나 있어 AURI 확인 요청 대상**입니다.
0-3. **브이월드 타일 확인** — 실제로 그려지는지, 인쇄에서 지도가 남는지. 좌표 계산은 실측으로 맞췄으나 **화면으로는 아직 못 봤습니다.**
0-1. **`server/legal.js` — `POLICY-72-73` 원문 대조** — 국정과제 이행계획서 확인 중(담당자). 그때까지 AI 의견에서 빠져 있습니다. 법령 2건은 조문 번호·제목만 확인된 상태라 **본문 인용을 막아 두었습니다.**
1. **브라우저에서 6단 리포트 실물 확인** — 문법·데이터는 검증했으나 **화면으로는 아직 못 봤습니다.** 서버 켜고 미추홀 조사지 안(위도 37.4596, 경도 126.6799) 지점으로 한 번 돌려 볼 것.
2. **4단계 렌더링 품질 재검증** — 지시문에 대비 요건을 넣었으므로 실제로 개선 전/후 차이가 보이는지 확인. 안 되면 **마스크(인페인팅) 방식**으로 전환.
3. **AURI에 확인할 것** (아래 "AURI 확인 대기" 참고)
4. **AI 진단 정확도 검증** — 미추홀구 사진 5~10장으로 `checklist.js` 보정. ⚠️ 항목별 판독으로 바꾼 뒤 실제 사진으로 아직 검증 안 됨.
5. **표준단가를 실제 값으로 교체** — 조달청 가격정보·지자체 일위대가. 비시설 사업 9종 단가도 임시값입니다.
6. 5단계 서술 문장 sLLM 연결 (과업3).
7. 시스템 개요·로직 다이어그램 3페이지 (중간보고 자료).
8. 과업요청서(RFP) 초안 — AURI 샘플 수신 후.

**AURI 확인 대기 (막힌 것은 아니고, 값만 교체하면 되는 것들)**
| | |
|---|---|
| 생활안전 편중 | 88%가 부천·안동·광주북구. 정제 과정 문제인지 원자료인지 |
| 읍면동 기준 | 사전진단서 3.2의 동별 건수를 어떻게 셌는지 (우리 값과 다름) |
| 자살 기준 | 구급출동만(926) vs 자살발생지점 포함(1,404) — 현재 926 |
| ~~자살 사고유형~~ | ✅ 2026-09-15 A_type 으로 공동주택·단독주택 구분 확보. 단 사전진단서 62.0%·10.2% 는 재현 안 됨(아래) |
| **화재 CSV 재전송** | `0826화재_유형.csv` 에 A_type 칸이 없고 type 칸이 덮어써짐. 지금은 0826 판과 줄 단위 결합 (담당자 문의 중) |
| **자살 62.0% 기준 · 발생지점 좌표** | 사전진단서 조사지 내 자살 장소 비율의 자료·분모. 자살발생지점 CSV 좌표 (담당자 문의 중) |
| **0904 표 ↔ 0909 CSV 판 차이** | 자살(지역 전체)·생활안전·화재(지역 전체) 건수가 두 판에서 다름 |
| **TIF 좌표계 · CULT_dist 단위** | TIF 에 .prj 없음(좌표 범위로 5186 가정). CULT_dist 값이 거리가 아닌 밀도처럼 보임 (담당자 문의 중) |
| **미수신 변수 32종** | 노후건축물·도로폭·경사·건물구조·토지피복 면적 등. 없어서 E 노후도·도로가 "미수신" (담당자 문의 중) |
| **행정동·법정동 연계표** | 연령별 인구(행정동)를 법정동에 **이름으로** 맞춤. 연계표(행안부)가 오면 못 붙인 동(부천 6곳 등)도 채우고 걸친 동 오차도 없앰 |
| 대응역량 자료 | 소방·경찰·응급의료 접근성, AED·CCTV 위치 |
| 건축물 용도 자료 | 자살 사고유형을 "공동주택"으로 쓰려면 필요 (2026-09-07 요청) |
| 발생장소·증상 분류 기준 | 119 원본의 `발생장`·증상 코드가 무엇을 묶은 것인지 (2026-09-07 요청). **묶음 이름을 정하려면 이것이 있어야 합니다** — 그때까지 원자료 값 그대로 |
| **강원대 분모 차이** | 연령총합이 우리보다 일관되게 적음 (부천 생활안전 16,207 vs 16,732). 0세·성별결측·연도 모두 아님 — 어떤 건을 뺐는지 (2026-09-08) |
| **겹치는 약어 6쌍** | `ROAD_2` vs `ROAD_2_P` 등이 한글 이름이 같은데 방향이 반대. 무엇이 다른지 (2026-09-08) |
| **★ 조사지내 교통사고 시트 오류** | 지자체 이름(가나다순)과 값(영문 지역코드순)이 어긋남. 41행 전부. **재전송 필요** (2026-09-08) |
| 사고유형 분류 기준 | 강원대가 화재·범죄·자살·산재·생활안전을 **재분류**했는데 기준 문서가 없음. 자살 `공동주택` 분류는 이걸 받으면 쓸 수 있음 |
| 지자체 조례 | 41개 지자체 재난안전 조례 이름·조 번호. 확인 전까지 AI 인용에서 제외 |
| 물적환경 나머지 34곳 | 회귀분석이 7개 대상지에만 있음. **미추홀(데모지)에 없음** |
| 팔레트 재정리분 | 생활안전·감염병의 H·A 사업 부족 (AURI 재정리 예정). `RX_WEAK` 4개 항목도 이때 채움 |
| 팔레트 596 vs 289 | 요약 시트 합계와 상세 시트 합계가 다름. 상세 시트를 정본으로 두었음 |
| ~~팔레트 신규/기존 판정~~ | ✅ 해결 — 메일 원문으로 확인(색 있는 쪽=기존). 2026-09-04 뒤집음 |
| 사업 금액 | 289종 중 15종만 단가로 사용 가능. 서식2 합계가 대부분 "산출 불가" |
| ~~`docs/` 커밋 여부~~ | ✅ 해결 — 2026-09-04 `docs/*` 로 통째 차단, `.gitkeep` 만 추적 |

---

## 9. 기술 스택

- 순수 HTML / CSS / JavaScript (프레임워크 없음 — 비전공자 유지보수 위해 단순 유지).
- 네이버 지도 API (NCP). 로드: `oapi.map.naver.com/openapi/v3/maps.js?ncpKeyId=<Client ID>&submodules=geocoder,panorama`
- 배포: GitHub Pages. 저장소: `akj-th/safetysimulator`.
- 지도가 뜨려면 네이버 콘솔 "Web 서비스 URL"에 배포 주소(github.io) 등록 필요.

---

## 10. 작업 방식 (중요)

- **설명은 쉬운 우리말로.** 담당자는 개발 비전공 — 전문용어는 풀어서, 필요하면 비유로.
- **코드를 바꾸기 전에** 무엇을·왜 바꾸는지 한두 줄로 먼저 설명.
- **과하게 복잡하게 만들지 않는다.** 지금은 핵심 흐름을 먼저 검증하는 단계.
- **가장 위험한 가정을 먼저 검증**하고(예: 렌더링 품질) 단계적으로 확장.
- 큰 방향(전체 구조·설계) 판단은 담당자와 함께. 임의로 아키텍처를 크게 바꾸지 않는다.

## 현재 상황 (2026-09-01 기준)

> 이 섹션이 가장 최신 정보입니다. 아래 내용이 위 항목들과 충돌하면 이 섹션을 따르세요.

### 결정된 사항

- **폐쇄망은 적용하지 않습니다.** AURI·AKJ 협의로 외부 API 활용 방식(A안)으로 확정.
  보안은 데이터 비식별화 · 승인제 접근통제 · 사용량 제한으로 보완합니다.
- 행안부 사무관·주무관 시연 완료, 반응 긍정적. **행안부 실장 참석 중간보고에서 시연 예정.**
- 본 도구의 위상: 완성형 시스템이 아니라 **데이터 분석 결과와 현장 의견을 연결하는 실증적 도구.**
  현장 방문 전 사전 탐지 및 사업 구상용입니다.

### 진행 중인 작업

중간보고 대비 업데이트가 진행 중입니다. 상세 내용은 아래 문서를 반드시 먼저 읽으세요.

```
docs/2026-09-01_업데이트_요구사항.md
```

핵심만 요약하면:
- 격자별 건수 표출에서 → **119 원본 속성값(연령·사고유형·시간대·장소) 분석 기반 리포트**로 전환
- 리포트를 6단 구성으로 개편 (인적특성 / 사고유형 / 물적환경 / 대응역량 / 현장확인 / 개선방향)
- 읍면동 단위 종합 분석 신규 추가
- 지자체별 확정 안전유형 3개 중심 서술

**진행 상황 (2026-09-04)**

| 작업 | 상태 |
|---|---|
| 1순위 인적요인·사고유형 통계 산출 | ✅ `tools/build-stats.mjs` · AURI 값과 295건 대조 오류 0 |
| 격자 집계 재생성 (옛 SHP → 새 CSV) | ✅ 종합 위험지수 분포가 크게 내려감 (미추홀 p50 34→20) |
| 2순위 리포트 6단 재편 | ✅ `report.html` + `assets/stats.js` |
| 3순위 읍면동 종합 분석 | ✅ 리포트 최상단. 좌표→법정동 조회 |
| 4순위 확정 안전유형 3개 반영 | ✅ 중점 3분야만 서술 |
| 5순위 개선 전/후 이미지 대비 강화 | ✅ 지시문에 대비 요건 추가 |
| 처방 아이템 → AURI 사업 팔레트 | ✅ 62종 → 289종. HEA 3분류·효과크기·출처 |
| ④ 대응역량 | ⏸ 자료 미수신 — "확인 필요" 표기 |
| **브라우저 실물 확인** | ⚠️ **아직 못 함.** 검증은 전부 통과했으나 화면으로 못 봄 |

⚠️ 미추홀 조사지 안 시험 좌표: **위도 37.4596, 경도 126.6799**

### 참조 문서 위치

| 경로 | 내용 |
|---|---|
⚠️ **`docs/` 는 .gitignore로 막혀 있습니다** (`docs/*`, `.gitkeep`만 예외). 아래 파일은 **각자 로컬에만 있습니다** — 저장소를 새로 받으면 폴더가 비어 있으니 따로 받아야 합니다.

| 경로 | 내용 |
|---|---|
| `docs/2026-09-01_업데이트_요구사항.md` | **작업 지시서 (최우선)** |
| `docs/20260819_컨소시엄_회의록.md` | 데이터 정제 현황·AI 도구 시연 |
| `docs/20260901_중간보고대비_화상회의록.md` | 중간보고 대비 업데이트 범위 |
| `docs/reference/데이터_분석_결과_보고서_보완사항_검토.pdf` | 행안부 주무관 요구사항 원문 |
| `docs/reference/250825_행정안전부_협의회의록.pdf` | 8/25 AURI-행안부 협의 |
| `docs/reference/사전진단서_경기 부천시.pdf` | 사람이 쓴 완성본 — 우리 리포트의 목표 형식 |
| `docs/reference/용역_과업지시서_sample.pdf` | RFP 작성 참고 (후속 과업) |

### 데이터 위치

| 경로 | 내용 | 비고 |
|---|---|---|
| `data/raw/incidents_0826/` | 119 출동자료 최종본 CSV 7종 | **개인정보 포함 · 외부 반출 금지** |
| `data/raw/analysis_0901/` | 강원대 분석 결과 XLSX + **사업 팔레트 XLSX** | 지자체별 안전유형·위험도 · 사업 289종 |
| `data/raw/analysis_0904/DB_약어.xlsx` | 물적환경 변수 약어 ↔ 한글 이름 | 7개 분야 시트 · 66개 변수 |
| `data/raw/인적 특성 결과_0904/` | 강원대 연령·성별 산출값 | ⚠️ 안에 세 폴더가 **한 번 더 중첩**돼 있음 |
| `data/raw/사고유형 특성_0904/` | 강원대 사고유형 산출값 | A_type 검증 기준 (조사지내 교통사고 시트는 정렬 오류) |
| `data/raw/analysis_0909/` | **A_type CSV 11종** · 분류 기준표 · 거리밀도 목록표 | 🔴 개인정보 포함 — 커밋 금지 |
| `data/raw/density_0909/` | 시설 밀도·거리 TIF 28종 × 41곳 (**23GB**) | .prj 없음 · 칸 10m/32.7m 혼재 |
| `data/raw/admin_boundary/` | 국토부 **법정 읍면동 경계** LSMD_ADM_SECT_UMD (시도별, EPSG:5186, EUC-KR) | 광주·전남은 옛 코드·통합특별시 두 벌 |
| `data/raw/population/` | 주민등록 202608 — 세대현황(총인구·남녀) · **연령별인구현황**(10세×남녀) | 행정동 단위 · H 는 연령별 파일 사용 |
| `data/dong/` | **동별 HEA 결과** (점수·비율·경계선) | ✅ 커밋 대상 · 개별 신고 없음 |
| `data/raw/물적환경_0904/` | 회귀분석 결과 (7개 대상지) | → `assets/physical.json` |
| `data/raw/gis_0901/` | 위험도 히트맵 TIF(6.6GB), 조사지 SHP | 조사지 = 지자체당 폴리곤 1개 |
| `data/incidents/` | 비식별 격자 집계 JSON (50m) | ✅ 새 CSV 기준으로 재생성 |
| `data/stats/` | 속성 분석 결과 (조사지 내/전체 비교·읍면동) | ✅ 생성됨 |
| `assets/palette.json` | AURI 사업 팔레트 289종 (비식별) | ✅ 커밋 대상 |
| ~~`data/heatmap/`~~ | 위험도 히트맵 PNG | 🗑 2026-09-07 삭제. 필요하면 `npm run heatmap` 으로 재생성 |

**`data/raw/`는 .gitignore로 차단되어 있습니다. 이 폴더의 파일을 밖으로 복사하거나 커밋하지 마세요.**
119 자료에는 환자 연령·성별·지번주소가 포함되어 있습니다.
웹앱이 사용하는 데이터는 반드시 전처리를 거쳐 개인정보를 제거하고 집계한 결과물이어야 합니다.

**서버는 화이트리스트 방식입니다** (2026-09-04) — `serveStatic`이 **아래 목록에 있는 것만** 내주고 나머지는 전부 403입니다. 2026-09-14 부터는 그 위에 **로그인 문**이 한 겹 더 있습니다(위 "계정 로그인").

```
최상단        *.html
assets/       .css .js .json .svg .png .jpg
data/incidents/  .json
data/stats/      .json
data/dong/       .json   (2026-09-15 동별 HEA)
```

차단 목록 방식이었을 때는 폴더가 새로 생길 때마다 다시 뚫렸습니다. 앱에 새 폴더가 필요해지면 `ALLOW_DIRS`에 한 줄 추가해야 하고, 깜빡하면 화면이 안 떠서 바로 알아챕니다(조용히 새는 것보다 낫습니다).

- 그 전까지는 데모 주소만 알면 `data/raw/`의 119 원본과 `server/.env`의 API 키를 그대로 받을 수 있었습니다. **렌더 접근 로그·API 사용량 확인 결과 외부 접근 기록은 없었습니다** (담당자 확인, 2026-09-04).
- 커밋 이력에도 없습니다 — `git log --all -- data/raw` 결과 없음.

**`docs/` 전체도 .gitignore로 막았습니다** — AURI 사전진단서·행안부 협의회의록(PDF)과 컨소시엄 회의록(.md)입니다. 참석자 실명과 내부 논의가 담겨 있어 `data/raw/`와 같은 이유입니다.

```gitignore
docs/*
!docs/.gitkeep
```

- **파일은 폴더에 그대로 둡니다.** 작업할 때 읽어야 하는 자료이고, 커밋만 막는 것입니다.
- `docs/`(슬래시로 끝)로 적으면 git이 폴더 안으로 들어가지 않아 `.gitkeep` 예외가 먹지 않습니다. 그래서 `docs/*`로 적었습니다.
- 폴더 구조와 이유를 남기려고 `docs/.gitkeep` 하나만 추적합니다.
- 확인 방법: `git add -An` — `.gitignore`와 `.gitkeep` 둘만 나와야 정상입니다.

### 2026-09-07 AURI 화상회의 — 반영 결과

| | 항목 | 상태 |
|---|---|---|
| 1 | ★ 리포트를 사전진단서 / 현장용 리포트 둘로 분리 | ✅ `document.html` 탭 2개 |
| 3 | 빈칸 → 편집칸 + AI 의견 + 법령 근거 | ✅ `doc-edit.js` · `/api/opinion` · `server/legal.js` |
| 4 | 현장 확인사항 카테고리화 | ✅ `field-check.js` 4범주 — 담당자 확인 완료(9/7). 네 번째를 **이용자 특성 관련 확인**으로 고치고 문장을 확인 행동 형태로 바꿈 |
| 6 | 개선 전/후 상하 배열 + 필수 문구 | ✅ 진단서·시각화 두 곳 |
| 7 | HEA 세 색 구분 + 부록 옅은 배경 | ✅ `theme.css` |
| 5 | 현장 사진 업로드 + EXIF | ✅ `site-photo.js` |
| 8 | 히트맵 오버레이 보류 | ✅ `index.html` 롤백. 스크립트는 남기고 **PNG 287장(19MB)은 삭제**(9/7 지시) |
| 9·10 | 건축물 용도 · 분류 기준 자료 | ⏸ AURI 회신 대기 |

### 동별 HEA 취약도 (2026-09-15) — `tools/build-dong-hea.mjs` → `data/dong/<지역>.json`

담당자 결정으로 만든 **임시 기준** 산출입니다. 기준값은 모두 파일 윗부분 상수에 근거와 함께 있습니다.

```
119 A_type CSV ─┐                                   ┌→ 지도 (index.html) H/E/A/종합 전환 · 경계선 + 점수 숫자
법정동 경계 SHP ─┼─ build-dong-hea.mjs → data/dong/ ─┤
밀도·거리 TIF  ─┤   (lib/zonal.mjs: TIF → 동 평균)   └→ 사전진단서 3.3 [표 2-1] 읍면동별 HEA 지표
회귀분석(지역 전체) ┘
```

| 축 | 계산 | 기준 |
|---|---|---|
| **H** 피해대상 | 분야별 연령(10년)×성별 집단마다 **기대 건수**(인구 기준: 동 거주 인구 × 나머지 지역 발생률 / 구성비 기준: 동 출동 수 × 나머지 지역 구성) → **실제 − 기대(초과 건수)가 가장 큰 집단의 초과 건수** → 백분위. 집단 3건·인구 100명 미만 제외 | 임시 |

**★ H 초과 건수 방식으로 변경 (2026-09-15 담당자 지적)** — `H_METHOD='excess'` · `H_COMPARE_REST=true`
- 지적: 부천 중동(중점 출동 508건) H **0점**, 심곡동(498건) **10점** — 출동이 가장 많은 동이 최하위.
- 원인 ① **큰 동이 지역 평균을 스스로 만듦** — 두 동이 중점 출동의 32%라 구성이 곧 지역 평균, 배수가 1.4~1.7 로 눌림.
  원인 ② **작은 동의 극단 배수** — 범박동 범죄 25건 중 "10세 미만 남성" 3건 → 15.1배 → H 100. 여러 집단 중 최대 배수를 고르니 표본이 작을수록 유리.
- 조치: 기대 건수를 **그 동을 뺀 나머지 지역**으로 계산 + 점수를 **배수 대신 초과 건수**로.
- 결과(부천): 중동 0→**95** · 심곡동 10→**90** · 상동 52→100 · 범박동 100→24 · 내동 76→5. 출동량과 H 순위상관 −0.42 → 0.88.
  출동량만 따라가지는 않음 — 괴안동 144건 H 29, 미추홀 용현동 199건 H 10(인구 기준, 쏠림 없음). 미추홀 순위상관 −0.54 → 0.14.
- 되돌리려면 `H_METHOD='ratio'`(배수) · `H_COMPARE_REST=false`(지역 전체 평균 비교).
| **E** 환경 | **Σ(β × z)** — β = ln(IRR) (강원대 **지역 전체** 회귀분석 유의 변수), z = 동 평균값의 지자체 표준점수. **TIF 있는 변수만** | 회귀분석 |
| **A** 행위·관리 | 반복 발생 지점 수 · 야간(22~06) 비중 · 1위 장소 비중(기타 제외) → 각 백분위 평균 | 임시 |
| **종합** | 중점 3분야 H·E·A 평균 → 지자체 안 백분위 → 있는 축끼리 평균 (**축 2개 미만이면 비움**) | 임시 |

- **동 단위 = 법정동** (`긴급_12`, 담당자 결정). 행정동 전환은 `DONG_UNIT` 상수 한 곳.
- **점수는 지자체 안 상대 순위(0~100)** — "위험/안전"이 아니라 **상위(67~)·중위·하위**로 부릅니다. 다른 지자체와 비교 불가.
- 표본: 동의 분야 출동 **10건 미만 "표본 부족"**(반복발생 포함), 편중 집단 **3건 미만 제외**, 위치 표시 **10건 미만 생략**.
- **값이 있는 동이 3곳 미만이면 순위를 매기지 않고 "비교 동 부족"** (`PERCENTILE_MIN_DONGS`). 1곳이면 무조건 50, 2곳이면 0·100 만 나와 순위라 부를 수 없기 때문입니다. 남해·태백·장흥처럼 농어촌은 중점 분야 출동이 동마다 적어 **대부분 비어 있습니다** — 실제 결과이며 채우지 않습니다.
- **산출 결과 (41곳)** — 동 954개 중 종합 점수 **381개**. 경계는 41곳 전부 매칭. 종합 점수가 하나도 없는 곳 4곳(장흥·제천·남해·태백).
  E 가 있는 7곳: 부천 22/24 · 광주북구 14/41 · **공주 3/37** · 관악 3/3 · 홍천 9/10 · 남원 34/41 · 영천 31/41 (종합 점수 동/전체).
  공주·광주북구는 동 대부분이 **E 만 있고 H·A 는 표본 부족**(중점 분야 출동 10건 미만)이라 종합이 빕니다 — 지도에서 E 로 전환하면 보입니다.
- **E 가 비는 경우는 이유를 적습니다** — 회귀분석 대상 아님(34곳) "자료 없음" · 유의 변수 TIF 미수신 "미수신". 평균값으로 채우지 않습니다(담당자 결정).
- 결과마다 근거가 남습니다 — 분야별 편중 집단·배수, 반복 지점 수, 야간 비율, 1위 장소, E 변수별 β·z·기여.

**동 경계 (`data/raw/admin_boundary/`, 2026-09-15 수신)** — 국토부 법정 읍면동 `LSMD_ADM_SECT_UMD`
- EPSG:5186(.prj 확인) · EUC-KR(`readDbf` 에 `encoding:'auto'` 추가 — 조사지 읽기는 그대로 확인).
- **지자체 ↔ 시군구 코드를 표로 적지 않고, 그 지자체 출동 지점의 5% 이상이 떨어지는 시군구 묶음**을 고릅니다. 광주·전남은 옛 코드(202606)와 "전남광주통합특별시"(202608) 두 벌이 겹쳐 와서, 이름이 80% 이상 겹치면 **최신 판**을 씁니다.
- 경계선은 8m 기준으로 단순화해 저장(부천 9,299 → 1,756 꼭짓점). **E 집계에는 원본 경계**를 씁니다.
- 출동자료의 동 이름이 경계에 없으면 `boundary.unmatched` 에 남깁니다(부천 "부천시" 1건 — 동 칸 오기).

**인구 통계 (`data/raw/population/202608_…_연령별인구현황_월간.csv`, 2026-09-15 수신)** — H 를 **인구 대비 출동률**로 계산합니다.
- 행정동 × 10세 구간 × 남녀 (EUC-KR). 80세 이상은 80~89·90~99·100+ 합산. 시군구 코드가 경계 파일과 같은 202608 판이라 코드로 지자체를 찾습니다(시 코드면 일반구까지).
- ★ **행정동 → 법정동 연계표가 없어 이름으로 맞춥니다** (`POPULATION_LINK_RULES`): 심곡1·2·3동→심곡동 · 돈암제1동→돈암동 · 숭의1.3동→숭의동 · 고강본동→고강동. 경계에 그 법정동이 있을 때만.
- ★ **이름 대응이 깨끗한 지자체에만 인구 기준을 씁니다** (`POPULATION_REQUIRE_CLEAN_LINK` — 행정동 전부 연결 + 법정동 전부 인구 있음).
  나머지는 **출동 구성비 기준**으로 두고 이유를 적습니다(`population.compositionReason`).
  - 이유(41곳 실측): 도시는 행정동 이름이 법정동과 달라 대부분 못 붙고(관악 행정동 2/21 · 대구 중구 법정동 3/57 · 영도 2/21),
    **공주는 행정동 16곳이 전부 붙었는데 법정동 37곳 중 16곳에만 인구가 몰려** 붙은 동의 인구가 부풀려짐(한 행정동이 여러 법정동에 걸침).
  - 섞어 쓰면(못 붙인 동만 비우는 방식) 종합 점수 동 381→322, 점수 0 지자체 4→6 이 되고, 붙은 동도 조용히 틀릴 수 있어 채택하지 않았습니다.
  - 부천: 법정동 18/24(성곡동·신흥동 행정동 못 붙임) → **구성비 기준**. 미추홀: 7/7·행정동 21/21 → **인구 기준**.
- ⚠️ **행안부 행정동·법정동 연계표**를 받으면 `linkPopulation()` 을 바꾸고 `POPULATION_REQUIRE_CLEAN_LINK` 를 풀 수 있습니다.
- ⚠️ 출동 **위치** 기준이라 방문자 출동도 들어갑니다 — 상업지·병원이 있는 동은 배수가 커집니다(시험 계산 때 부천 소사동 자살 10대 남성 30배: 3건, 발생 장소 50% 의료기관).
- 문장에 기준을 드러냅니다 — 인구 기준 "거주 인구 대비 출동률이 지역 평균의 N배(1천 명당 연 x건)", 구성비 기준 "출동 환자 중 비중 … (인구 대비 아님)". 리포트 ① 문장도 "구급출동 환자 중 ○○ 비율"로 고쳤습니다(거주 비중과 혼동 방지).

**★ 웹 표시 — 산출 근거만 (2026-09-15 담당자 지시, 중간보고용)**
- 웹에는 **비어 있음·자료 없음·표본 부족 같은 문구를 보이지 않습니다.** 무엇으로 계산했는지(`basisNotes`)만 적고, 마지막 줄 `* 해당 기능은 구현중이므로 정확하지 않을 수 있음`.
- 지도: 점수가 하나도 없는 지자체는 기능 자체를 감춤 · 점수 없는 축 버튼 감춤 · 점수 없는 동은 경계선만(누를 수 없음) · 말풍선은 있는 점수·근거만.
- **지도 표현 — 축마다 색 하나 + 진하기(투명도) + 동 이름·점수 숫자** (숫자 색은 상위·중위·하위). H `#D83D64` · E `#333333` · A `#2793C9` · 종합 `#000000`, 투명도 0.06~0.62 (`fillOf`). 범례 막대 "낮음→높음". ※ 9/7 "면으로 덮지 말 것" 과 다르지만 담당자 지시.
- ★ **읍면동별 HEA 지표 표는 위치 선택 화면(index.html)으로 옮겼습니다** (2026-09-15 담당자 지시) — **사전진단서 3.3 절은 삭제**(사전진단서는 다시 AURI 원문 구성만).
  - "동별 HEA 취약도"에서 H·E·A·종합 중 하나를 켜면 **지도·스트리트뷰 아래**에 표 패널이 뜨고, 끄기면 사라짐. 켠 축의 행은 옅게 강조.
  - 표는 `AuriDongHea.renderTable(data, {mode})` 한 곳. 종합 점수가 있는 동만 열로(조사지와 겹치는 동 먼저, 최대 12곳) · **지표 행 8개는 모두 유지, 값 없는 칸은 "-"** · 점수 없는 지자체는 표 없음.
  - 각주는 산출 방법 두 줄 + 마지막 `* HEA 취약도 지표는 현재 개발 중인 모델로 산출한 참고값으로, 정확하지 않을 수 있음`. 지도 패널의 산출 근거 목록은 이 각주로 합쳤습니다.
  - 보조 절 표기·비교 불가·임시 기준·빈칸 범례 문구는 넣지 않음(담당자 지시).
- 빈 이유를 적던 함수(`caveats`·`emptyReason`·`axisReason`·`scoreSummary`)는 **다시 켤 수 있게 남겨 둠**(지금 웹에서 안 씀).
- 비어 있는 자료 목록은 `docs/2026-09-15_동별HEA_비어있는자료.md`(로컬, 커밋 안 됨). **기준은 낮추지 않습니다**(담당자 결정).

**3.3 머리 한 줄** — (삭제됨, 표는 위치 선택 화면으로 이동)

**E 반영 변수 (지역 전체 회귀분석 기준)** — 담당자가 적은 "화재 0/3 · 생활안전 0/6 · 교통사고 1/5"는 **조사지 내** 모형 기준이고, 결정대로 지역 전체 모형을 쓰니 숫자가 다릅니다. 부천: 범죄 6/9 · 감염병 4/7 · 자살 8/10 · 교통사고 7/18 · **화재 0/7 · 생활안전 0/7** · 산재 1/3.
**노후도·도로 지표는 거의 모두 "미수신"** — 노후건축물·도로폭 TIF 가 없습니다(도로는 교차로 밀도 하나).

**⚠️ 사전진단서에 넣은 3.3 절은 AURI 원문에 없는 절입니다** — 담당자가 명시적으로 요청해 넣었습니다(원문 밖 요소 금지 원칙의 예외). 표 번호는 기존 번호가 밀리지 않게 `[표 2-1]`.

**⚠️ 지도 표현** — 9/7 회의 "면으로 덮으면 짚기 어렵다"에 따라 **경계는 선만, 점수는 숫자만, 색은 글자에만**. 격자 점 레이어와 따로 켜고 끕니다. E 가 없는 지자체는 E 버튼이 잠깁니다.

### 2026-09-14 AURI 회의 — 반영 결과

| | 항목 | 상태 |
|---|---|---|
| 1 | 진단서 탭 순서 — 현장용 리포트 → 사전진단서 | ✅ 탭 순서 + 처음 열리는 탭 |
| 2 | ★ 계정 기반 승인제 로그인 | ✅ `server/auth.js` · `login.html` · `admin.html` · `assets/auth.js`. 메모리 DB로 흐름 53건 + 방식 전환 13건 확인. **렌더 DB 설정은 담당자** |
| 3 | ★ 면책 문구 상시 표기 | ✅ `AURI_AI_DISCLAIMER` 한 곳 · 시각화 + 현장용 리포트 · 인쇄 포함 |
| 4 | 개선 이미지 강조 톤 | ✅ 지시문 조정 — **결과 확인은 담당자** |

### 계정 로그인 (2026-09-14) — `server/auth.js`

8/25 협의의 **"승인제 회원가입 + 로그인 기반 접근통제"** 입니다.

```
가입 신청(아이디·비밀번호·이름·소속) → pending → 관리자 승인 → active → 로그인
                                        └ 거부 → rejected   └ 비활성화 → disabled (즉시 로그아웃)
```

**방식 스위치 — `AUTH_MODE`** (문제가 생기면 이 한 줄로 되돌립니다)

| 값 | 무엇 | 언제 |
|---|---|---|
| `account` | 계정 로그인 (**기본값**) | 렌더 |
| `code` | 예전 `ACCESS_CODE` 방식 — 코드를 지우지 않고 남겨 둠 | **되돌리기** |
| `off` | 확인 안 함 | 내 컴퓨터 개발용만 |

⚠️ `account` 인데 DB 에 못 붙으면 **사이트를 닫습니다**(503 "설정 필요" 화면). 열어 두면 로그인 없이 누구나
들어오기 때문입니다. DB 가 늦게 깨어나면 30초마다 다시 붙어 보고, 붙는 순간 저절로 열립니다.

**서버가 문을 지킵니다** — 브라우저 쪽에서만 막으면 주소를 직접 쳐서 들어올 수 있기 때문입니다.

| 로그인 전에도 받을 수 있는 것 | 로그인 뒤에만 |
|---|---|
| `login.html` · `assets/` 의 css·js·svg·png·jpg | 나머지 모든 `.html` · `data/` · `assets/*.json` · 모든 `/api/` |

- 로그인 안 한 채 화면을 열면 `login.html?next=…` 로 보내고, 로그인하면 원래 화면으로 돌아옵니다
- `admin.html` 과 `/api/admin/*` 은 **관리자만**. 일반 사용자는 첫 화면으로 돌려보냄
- 화면(HTML)에는 `Cache-Control: no-store` — 로그아웃 뒤 "뒤로 가기"로 다시 보이지 않게

**비밀번호** — bcrypt 해시만 저장 (`bcryptjs`, 비용 12). 원문은 DB·로그·화면 어디에도 없습니다.
`bcryptjs` 는 bcrypt 알고리즘의 순수 자바스크립트 구현이라 해시 형식(`$2b$12$…`)이 같고 컴파일 없이 설치됩니다.

**세션 — 마지막 활동 후 30분** (`SESSION_MINUTES`)

| | 만료가 30분 뒤로 밀리나 |
|---|---|
| 화면 열기 · AI 기능 사용 · **로그인 유지** 버튼 | ✅ |
| 화면이 1분마다 남은 시간 확인(`/api/auth/me`) · 자료(.json) 받기 | ❌ — 창만 열어 두면 끝나야 하므로 |

- 상단 바 위 **계정 줄**에 이름·아이디·소속, 남은 시간, 로그인 유지, (관리자면) 계정 관리, 로그아웃
- **5분 남으면** 계정 줄이 경고로 바뀌어 "로그인 유지"를 누르라고 안내 — 요청의 "로그인 유지 확인 기능"을 이렇게 해석했습니다
- 브라우저에는 무작위 열쇠(쿠키 `auri_sid`, HttpOnly·SameSite=Lax·https에서 Secure)만, DB 에는 그 열쇠의 **해시**만 둡니다

**틀리지 않게 한 것**
- 로그인은 **비밀번호를 먼저** 확인하고, 맞을 때만 "승인 대기 중입니다" 를 알려 줍니다. 순서가 거꾸로면 비밀번호 없이 가입 여부를 알아낼 수 있습니다. 없는 아이디·틀린 비밀번호는 같은 문구, 같은 응답 시간
- 같은 IP 에서 **10번 틀리면 15분 차단**
- 관리자 계정은 **화면에서 끌 수 없습니다** (실수로 모두 잠기는 것 방지). `ADMIN_ID`·`ADMIN_PW` 환경변수로만 — 서버가 켜질 때 없으면 만들고, **있으면 비밀번호를 환경변수 값으로 다시 맞춥니다**(잊으면 환경변수만 바꾸면 됨)
- 비활성화하면 이미 열린 세션도 **즉시** 끊습니다
- 접속자 IP 는 Cloudflare 가 덮어쓰는 `CF-Connecting-IP`·`True-Client-IP` 를 먼저 씁니다 (`X-Forwarded-For` 첫 칸은 접속자가 꾸며 보낼 수 있음). 요금 상한도 같은 함수

**기록 (DB 표)**

| 표 | 무엇 |
|---|---|
| `users` | 계정 · 상태 · 누가 언제 승인/거부/비활성화했는지 · 마지막 로그인 |
| `sessions` | 열려 있는 로그인 (열쇠 해시 · 만료) |
| `login_log` | **계정 · 시각 · IP · 성공 여부 · 실패 사유**(비밀번호 틀림/승인 대기/비활성화/거부) |
| `diagnosis_log` | **계정 · 시각 · 좌표 · 지자체 · 주소 · IP · 구분**(진단/재판독) |

- 표는 서버가 켜질 때 `CREATE TABLE IF NOT EXISTS` 로 **알아서** 만듭니다. DB 에 따로 들어갈 필요 없음
- 진단 기록은 요청이 제대로 갖춰진 뒤 **AI 를 부르기 직전**에 남깁니다. AI 가 실패해도 "시도했다"는 남습니다
- 지자체는 화면이 보낸 주소를 `data/stats/index.json` 의 41개 지자체 이름과 맞대어 찾습니다(긴 이름부터 — "중구" 오인 방지). 못 찾으면 비워 둡니다
- 관리 화면에서 최근 300건을 보고, **CSV(최근 5,000건)** 로 내려받습니다. 엑셀 한글이 깨지지 않게 BOM, 시각은 한국 시간

**렌더 설정 절차** (담당자)
1. Dashboard → New + → **Postgres** · Region 은 **웹 서비스와 같은 지역** · Plan Free → Available 될 때까지 대기
2. DB → Connections → **Internal Database URL** 복사 (비밀번호가 들어 있음)
3. 웹 서비스 → Environment: `DATABASE_URL` · `ADMIN_ID` · `ADMIN_PW` · `AUTH_MODE=account` (`ACCESS_CODE` 는 지우지 말 것)
4. Settings: Root Directory `server` · Build Command `npm install` 확인 (`pg`·`bcryptjs` 가 새로 들어옴)

⚠️ **렌더 무료 PostgreSQL 은 만든 지 30일 뒤 만료됩니다**(생성 화면 안내 확인). 로그인·진단 기록을 보존하려면 그전에 유료로 올려야 합니다.

**시험한 방법** — 로컬에 PostgreSQL 이 없어 **메모리 PostgreSQL(pg-mem)** 을 실제 서버에 끼워 돌렸습니다(시험 도구는 저장소에 넣지 않음).
로그인 전 차단 13 · 가입 5 · 승인 전 4 · 관리자 8 · 승인 후 7 · 진단 기록 6 · 유지·비활성·로그아웃 7 · 대입 차단 1, 그리고 만료(3초로 줄여서)·DB 없음·code 되돌리기 13건.
⚠️ **진짜 PostgreSQL 에서는 아직 안 돌려 봤습니다** — 쓴 SQL 은 표준 문법뿐이지만, 렌더 설정 뒤 첫 로그인이 실제 확인입니다.

### 프로젝트 저장 · 불러오기 (2026-09-15) — `server/projects.js`

진단 한 건은 탭(sessionStorage)에만 있어 탭을 닫으면 사라졌습니다. 계정별로 DB 에 저장해 다시 엽니다.
(9/7 AURI 의 "기록이 쌓이는 구조" 요청 중 **저장 위치를 서버로** 한다는 부분이 여기서 해결됐습니다.
같은 지점의 방문 기록을 시간순으로 모아 보는 기능은 아직 없습니다 — 아래 "사진 누적 구조" 참고)

**계정 줄**: `… 로그인 유지 · **프로젝트 저장** · **내 프로젝트** · (계정 관리) · 로그아웃` + 지금 열린 프로젝트 이름

**무엇을 저장하나** — 진단 한 건을 이루는 브라우저 저장 칸 10개 (`SAVE_KEYS`)

```
auri_picked_location  auri_input_image   auri_diagnosis_results  auri_revision_log  auri_rx_overrides
auri_prescriptions    auri_region_stats  auri_after_image        auri_doc_notes     auri_site_photos
```

- ★ **목록이 두 곳에 있습니다** — `server/projects.js` 와 `assets/projects.js`. 앱에 새 저장 칸이 생기면 **둘 다** 넣어야 저장됩니다. 안 넣으면 조용히 빠집니다
- 서버는 이 목록에 있는 **문자열만** 받습니다. 다른 칸(로그인 표시·접속 암호 등)은 보내도 버립니다
- 지자체·진행 단계·판독 요약(위험 N · 주의 N · 안전 N)은 **서버가 내용에서 계산**합니다 (화면이 보낸 값을 믿지 않음)

**누가 무엇을 할 수 있나**

| | 내 프로젝트 | 남의 프로젝트 |
|---|---|---|
| 일반 사용자 | 열기 · 저장(덮어쓰기) · 삭제 | ✗ — 목록에도 없고, 번호로 열어도 "없음" |
| 관리자 | 열기 · 저장 · 삭제 | **열람만** ("전체 사용자" 탭, 작성자로 거르기). 저장하면 **관리자 이름으로 새로 복사** |

- 관리자가 남의 것을 덮어쓰거나 지우지 못하게 한 이유: 그 안의 판독 보정·문서 글은 **작성자의 판단 기록**입니다. 덮어쓰면 누가 판단했는지 되짚을 수 없습니다. 서버가 막습니다(403/404)
- 비활성화된 사용자의 프로젝트도 관리자는 계속 열람할 수 있습니다

**저장 창**
- 이름(비우면 지자체 이름) · 내 프로젝트를 연 탭이면 **덮어쓰기 / 새로 저장** 선택
- 프로젝트를 연 뒤 **진단 위치가 바뀌었으면 "새로 저장"이 먼저 골라집니다** — 다른 지점의 진단으로 예전 프로젝트를 덮어쓰는 실수 방지
- 현장 사진이 있으면 "함께 저장" 체크(기본 켬) + **사람·차량번호가 서버에 저장된다**는 안내
- 저장 크기 표시. 한 건 **25MB** 한도

**열기**
- 지금 탭에 다른 진단이 있으면 확인을 묻고, **10칸을 모두 비운 뒤** 채웁니다(앞 진단의 개선 후 이미지가 섞여 남지 않게)
- 판독 결과가 있으면 **리포트(3단계)**, 위치만 있으면 진단(2단계)으로 갑니다
- 브라우저 저장 공간(보통 5MB)을 넘으면 반쯤 채운 상태를 남기지 않고 다시 비운 뒤 안내합니다

⚠️ **DB 용량** — 이미지가 들어 있어 한 건이 수백 KB ~ 수 MB 입니다. 렌더 무료 DB(1GB)면 **대략 수백 건**이 한도입니다. 목록에 건별 크기가 나옵니다. 30일 만료와 함께 유료 전환을 검토할 이유가 하나 더 생겼습니다.

**시험** — 메모리 PostgreSQL 로 서버 31건(저장·목록·열기·덮어쓰기·관리자 열람·삭제·비활성 사용자) + `assets/projects.js` 를 가짜 브라우저 저장소로 돌려 11건(찌꺼기 제거·취소·공간 초과·열린 프로젝트 삭제). 기존 로그인 시험 52건 영향 없음. 저장 창 화면 자체는 브라우저로 못 봤습니다.

### 2026-09-08 추가 반영 (9/7 회의 후속 + 0904 신규 자료)

| | 항목 | 상태 |
|---|---|---|
| 1 | AI 결론부 — (소결) 자리 · 조례 처리 | ✅ 조례를 `ordinance` 유형으로 추가하되 **미확인이라 인용 제외**. AI 가 쓸 수 있는 것은 법령 2건 + 협의 2건 |
| 3 | "교통지역" 표기 삭제 | ✅ `PLACE_GROUPS` 비움 · `PLACE_CHECKS` 두 줄로 · 재생성 후 검증 295건 오류 0 |
| 4 | 물적환경 회귀분석 반영 | ✅ `build-physical.mjs` → `physical.json` · 4장에 `(관련요인)` 줄 추가 (7개 지자체만) |
| a | 강원대 연령·성별 대조 | ✅ `compare-kangwon.mjs` — 10년 단위 칸은 일치, 분모 차이는 원인 미상 |
| a | 강원대 사고유형 대조 | ✅ `compare-kangwon-type.mjs` — 감염병·교통사고 일치. **조사지내 교통사고 시트 오류 발견** |
| c | 연령 구간 | ✅ **현재 방식(20세 포함) 유지** — 담당자 결정(9/8). 20세가 누락되는 구조보다 낫고 295건 재검증도 피함 |
| 2 | 사진 누적 구조 | ⏸ **구조 제안만** — 서버 저장이 필요해 이번에는 미룸 |

⚠️ **아직 브라우저로 못 봤습니다.** 문법·정합성 검증(verify-rules 0건 · verify-stats 295건 0건 ·
인라인 스크립트 5개 문법 통과 · 서버가 새 파일 200 응답)은 통과했지만 화면과 인쇄
미리보기는 담당자 확인이 필요합니다.

### 사진 누적 구조 — 제안 (2026-09-07 AURI 요청, 아직 만들지 않음)

> "사전 진단을 계속하면 할수록 현장 기록이 쌓여서, 이 지역 문제가 해소됐는지
>  판단 가능한 툴로 진화"

지금 사진은 `sessionStorage` 에 있습니다. **탭을 닫으면 사라집니다** — 누적이 될 수 없습니다.
누적하려면 저장 위치가 서버여야 하고, 그러면 아래가 함께 결정돼야 합니다.

| 정해야 할 것 | 왜 |
|---|---|
| **어디에 저장하나** | 배포처(Render)는 디스크가 껐다 켜면 지워집니다. 별도 저장소가 필요 |
| **개인정보** | 현장 사진에는 사람·차량번호가 찍힙니다. `data/raw/` 와 같은 등급의 자료가 됩니다 |
| **누가 볼 수 있나** | 지금 서버는 암호가 없습니다(`ACCESS_CODE` 미설정). 사진을 쌓으면 접근통제가 먼저입니다 |

만든다면 이런 모양입니다 (좌표를 열쇠로 삼는 것이 핵심).

```
POST /api/site-photos   { lat, lng, dataUrl, takenAt, note }
GET  /api/site-photos?lat=&lng=&radius=100   → 촬영시각 오름차순
```

- **같은 지점 판정은 좌표 반올림**으로 합니다 (격자와 같은 50m). GPS 오차가 있어
  "정확히 같은 좌표"로는 절대 안 묶입니다.
- 촬영시각은 이미 EXIF 에서 읽고 있습니다(`site-photo.js`) — 정렬 열쇠가 이미 있습니다.
- 화면은 4장 사진 자리에 **"이 지점 이전 기록 3건"** 을 띄우고 시간순으로 늘어놓으면 됩니다.

**권장**: 9/17 중간보고까지는 지금 구조(세션 저장)로 두고, 접근통제와 저장 위치를
정한 뒤에 붙이는 편이 안전합니다. 개인정보가 실린 사진을 암호 없는 서버에 쌓기 시작하면
되돌리기 어렵습니다.

### 후속 과업 (중간보고 이후)

- **과업 요청서(RFP) 작성** — 행안부가 내년 사업 발주 시 사용할 제안요청서.
  폐쇄망/오픈망 기술 비교 포함. 최종보고서 부록 형태.