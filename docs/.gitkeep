이 폴더의 내용은 일부러 커밋하지 않습니다 (.gitignore 의 `docs/*`).

AURI 사전진단서, 행안부 협의회의록, 컨소시엄 회의록이 들어 있습니다.
참석자 실명과 내부 논의가 담겨 있고, 이 저장소는 GitHub Pages 로
공개되므로 data/raw/ 와 같은 이유로 막았습니다.

파일 자체는 각자 로컬에 그대로 두고 작업할 때 참고합니다.
공유가 필요하면 저장소가 아닌 다른 경로로 주고받으세요.

폴더 구조만 남기려고 이 파일 하나만 예외로 두었습니다.
